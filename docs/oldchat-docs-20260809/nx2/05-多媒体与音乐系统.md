# 05 - 多媒体与音乐系统 (v1.4.x)

> 基于 jadx 反编译分析  
> 更新时间: 2026年8月

---

## 1. 音乐播放系统

### 1.1 架构概览

```
MusicPlazaActivity (音乐广场)
    ↓ 选择歌曲
MusicPlayerActivity (播放界面)
    ↓ Intent
MusicPlaybackService (后台播放服务)
    ├── MediaPlayer (音频播放)
    ├── AudioManager (音频焦点)
    ├── 通知栏控制
    └── 文件缓存管理

v1.4.x 新增:
MusicSearchActivity (音乐搜索, jadx确认)
    ↓ 搜索框+结果列表+下载管理
MusicCategoryActivity (音乐分类, jadx确认)
    ↓ 分类浏览，支持分页加载
PlaylistDetailActivity (播放列表详情, jadx确认)
    ↓ 列表名/歌曲数/时长，支持编辑/删除/添加
```

### 1.2 MusicPlaybackService 详细分析

#### 1.2.1 核心状态

| 字段 | 类型 | 说明 |
|---|---|---|
| f2268a | MediaPlayer | 播放器实例 |
| f2269b | boolean | 是否正在加载 |
| f2270c | int | 播放会话ID (防并发) |
| f2271d | int | 总时长 (ms) |
| f2272e | int | 当前位置 (ms) |
| f2273f | boolean | 单曲循环 |
| f2274g | String | 歌曲名称 |
| f2275h | String | 歌曲URL |
| f2276i | String | 封面URL |
| f2277j | String | 歌词URL |
| f2278k | String | 作者UID |
| f2279l | String | 作者名称 |
| f2280m | String | 作者头衔 |
| f2281n | String | 作者头像 |
| f2282o | String | 条目ID |
| f2283p | int | 点赞数 |
| f2284q | int | 评论数 |
| f2285r | boolean | 是否已点赞 |

#### 1.2.2 播放流程

```
1. 接收 PLAY_SONG Intent
2. 解析歌曲信息 (name, url, cover, lyrics, owner等)
3. 检查是否同一首歌 (URL比较)
   ├── 是: 更新元数据，继续播放
   └── 否: 停止当前播放，开始新歌
4. 检查本地缓存
   ├── 有缓存: 从文件播放 (U方法)
   └── 无缓存: 流式播放 (V方法) + 后台缓存
5. 设置 MediaPlayer:
   - AudioAttributes: USAGE_MEDIA, CONTENT_TYPE_MUSIC
   - 数据源: 文件路径 或 URL
   - 监听器: OnPrepared, OnCompletion, OnError
6. prepareAsync → onPrepared → start
7. 启动进度更新线程 (500ms间隔)
8. 显示前台通知
```

#### 1.2.3 缓存策略

**缓存目录**: `{filesDir}/music_player_cache/`

**缓存文件命名**: `SHA1(url) + 扩展名`

**扩展名检测**:
```java
String[] extensions = {".mp3", ".m4a", ".aac", ".amr", ".3gp", ".wav", ".ogg", ".flac"};
```

**缓存条件**:
- WiFi环境: 自动缓存
- 非WiFi: 仅在用户主动下载时缓存

**缓存清理** (LRU):
- 最大大小: 220MB (230686720 bytes)
- 最大文件数: 120
- 清理策略: 按 `lastModified` 排序，删除最旧的

#### 1.2.4 音频焦点管理

```java
public void onAudioFocusChange(int focusChange) {
    if (focusChange == AUDIOFOCUS_LOSS_TRANSIENT) {
        f2287t = isPlaying();
        pause();
    } else if (focusChange == AUDIOFOCUS_LOSS) {
        f2287t = false;
        pause();
    } else if (focusChange == AUDIOFOCUS_GAIN && f2287t) {
        f2287t = false;
        play();
    }
}
```

#### 1.2.5 通知栏控制

| 按钮 | Action | 图标 |
|---|---|---|
| 播放/暂停 | TOGGLE_PLAY | ic_media_play/pause |
| 单曲循环 | TOGGLE_REPEAT_ONE | ic_menu_rotate |
| 关闭 | STOP | ic_menu_close_clear_cancel |

---

## 2. 音乐系统重大增强 (v1.4.x, jadx 确认)

### 2.1 播放列表系统

| 功能 | API端点 | UI组件 | 说明 |
|---|---|---|---|
| 播放列表CRUD | `/music/playlists` | PlaylistDetailActivity | 创建、读取、更新、删除播放列表 |
| 播放列表同步 | `/music/playlists/sync` | — | 跨设备同步播放列表 |
| 播放列表详情 | — | PlaylistDetailActivity | 显示列表名/歌曲数/时长，支持编辑/删除/添加 |

### 2.2 歌词系统 (jadx 确认)

| 功能 | API端点/组件 | 说明 |
|---|---|---|
| 歌词获取 | `/music/plaza/lyrics?item_id=` | 从服务器获取歌词 |
| 歌词显示 | `LyricCascadeView` | 歌词级联视图 (含内部类 $a-$n，支持高亮当前行) |
| 歌词弹跳效果 | `music_lyric_bounce_enabled` | 歌词弹跳动画 |
| 歌词颜色模式 | `music_lyric_color_mode` | 自定义歌词颜色 |

### 2.3 音乐搜索与分类 (jadx 确认)

| 功能 | 组件 | API端点 | 说明 |
|---|---|---|---|
| 音乐搜索 | `MusicSearchActivity` | `/music/plaza?limit=50&offset=0&sort=latest&q=` | 搜索框+结果列表+下载管理 |
| 音乐分类 | `MusicCategoryActivity` | — | 分类浏览，支持分页加载 |
| 音乐详情 | — | `/music/plaza/detail?item_id=` | 获取音乐详细信息 |
| 最新音乐 | — | `/music/plaza?limit=50&offset=0&sort=latest` | 最新音乐列表 |

### 2.4 视觉增强 (jadx 确认)

| 功能 | 组件 | 说明 |
|---|---|---|
| 封面流视图 | `CoverFlowView` | 封面流3D翻转效果 (含内部类 $a-$i，9个内部类) |
| 封面氛围动效 | `CoverAmbientMotionView` | 背景色彩随封面变化 |
| 动态背景 | `music_dynamic_bg_enabled` | 音乐播放时动态背景 |
| 背景样式 | `music_bg_style` | 自定义背景样式 |
| 圆形图片 | `CircleImageView` | 圆形封面/头像显示 |

### 2.5 音乐API端点汇总 (jadx grep 确认)

| 端点 | 方法 | 说明 | 版本 |
|---|---|---|---|
| `/music/plaza?limit=30&offset=` | GET | 音乐广场分页 | — |
| `/music/plaza/ranking?limit=50` | GET | 排行榜 (旧版仅limit=10) | v1.4.x扩展 |
| `/music/plaza/detail?item_id=` | GET | 音乐详情 | v1.4.x新增 |
| `/music/plaza/lyrics?item_id=` | GET | 歌词获取 | v1.4.x新增 |
| `/music/plaza?limit=50&offset=0&sort=latest` | GET | 最新音乐 | v1.4.x新增 |
| `/music/plaza?limit=50&offset=0&sort=latest&q=` | GET | 音乐搜索 | v1.4.x新增 |
| `/music/playlists` | GET/POST/PUT/DELETE | 播放列表CRUD | v1.4.x新增 |
| `/music/playlists/sync` | POST | 播放列表同步 | v1.4.x新增 |
| `/music/plaza/like` | POST | 音乐点赞 | — |
| `/music/plaza/unlike` | POST | 取消点赞 | — |
| `/music/plaza/comment` | POST | 音乐评论 | — |
| `/music/plaza/comments?item_id=` | GET | 音乐评论列表 | — |

---

## 3. 资源上传系统

### 3.1 ResourceUploadService 详细分析

#### 3.1.1 上传流程

```
1. 接收 RESOURCE_UPLOAD_START Intent
2. 检查是否忙碌 (l()方法)
   ├── 忙碌: 返回 "upload_busy" 错误
   └── 空闲: 继续
3. 检查网络状态 (G.d().f())
   ├── 不可用: 返回 "network_unavailable"
   └── 可用: 继续
4. 检查认证Token
   ├── 无Token: 返回 401 "no_token"
   └── 有Token: 继续
5. 构造上传请求:
   - 数据源: ContentResolver.openInputStream(uri)
   - 参数: section_id, file_name, content_type
   - API: POST /resources/upload
6. 启动前台通知 (进度条)
7. 执行上传 (g0.d.U方法)
8. 进度回调 → 更新通知 + 发送广播
9. 完成/失败 → 发送广播 + 停止服务
```

#### 3.1.2 并发控制

使用静态锁机制，同一时间只允许一个上传任务。

---

## 4. 图片处理

### 4.1 自定义控件 (jadx 确认)

| 控件 | 功能 | 版本 |
|---|---|---|
| **ZoomImageView** | 支持双指缩放的图片视图 | — |
| **RoundedImageView** | 圆角图片控件 | — |
| **CircleImageView** | 圆形图片裁剪显示 | **v1.4.x新增** |

### 4.2 图片预览

`ImagePreviewActivity` 提供全屏图片预览功能，支持:
- 全屏显示
- 双指缩放
- 保存到本地

---

## 5. 音频录制

### 5.1 权限

- `RECORD_AUDIO` — 录音权限 (运行时权限)

### 5.2 推测功能

基于权限和 Activity 分析，录音功能可能用于:
- 语音消息 (ChatActivity)
- 语音动态 (MomentComposeActivity)

---

## 6. 视频播放

### 6.1 B站视频

OldViewVideoFullActivity 配置:
- `screenOrientation="landscape"` — 横屏锁定
- `theme="@style/AppTheme.Fullscreen"` — 全屏主题

v1.4.x 新增 `OldViewPlayerView` 控件 (jadx 确认)，提供更专业的播放器体验。

### 6.2 聊天视频

ChatActivity 支持视频消息的发送和播放。
