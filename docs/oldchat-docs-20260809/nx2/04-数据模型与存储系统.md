# 04 - 数据模型与存储系统 (v1.4.x)

> 基于 jadx 反编译分析  
> 更新时间: 2026年8月

---

## 1. 存储架构概览

```
┌─────────────────────────────────────────┐
│              数据存储层                   │
├─────────────┬───────────────┬───────────┤
│ SharedPrefs │   SQLite DB   │ File Store│
│  (轻量KV)   │  (结构化数据)  │ (大文件)  │
├─────────────┼───────────────┼───────────┤
│ auth        │ 聊天记录       │ 音乐缓存  │
│ 好友请求    │ 好友列表       │ 图片缓存  │
│ 通知状态    │ 群组信息       │ 上传临时  │
│ 用户偏好    │ 动态数据       │ Lua脚本   │
│ BiliAuth    │ 音乐播放列表   │ B站缓存   │
│ NCUID存储   │               │ 证书文件  │
└─────────────┴───────────────┴───────────┘
```

---

## 2. SharedPreferences 详细分析

### 2.1 auth — 认证信息

| 键名 | 类型 | 说明 |
|---|---|---|
| `access_token` | String | API认证Bearer Token |
| `refresh_token` | String | Token刷新令牌 |
| `user_id` | String | 用户数据库ID |
| `my_uid` | String | 用户唯一标识UID |
| `my_ncuid` | String | 用户NCUID (v1.3.61+) |
| `KEY_ACCESS_TOKEN` | String | Token存储键 (v1.4.x配置化) |
| `saved_username` | String | 记住的用户名 (**明文**) |
| `saved_password` | String | 记住的密码 (**明文**) |
| `token` | String | 旧版token键名 (兼容) |

### 2.2 好友请求缓存 (i0.b)

| 键名 | 类型 | 说明 |
|---|---|---|
| 好友请求未读数 | int | 本地缓存的好友请求数量 |

### 2.3 通知已读状态 (i0.f)

| 键名 | 类型 | 说明 |
|---|---|---|
| 通知已读ID | Set/String | 已读通知的ID记录 |

### 2.4 用户偏好

| SP名称 | 管理类 | 说明 |
|---|---|---|
| (未知) | `o0.V` | 隐私协议同意状态、用户偏好 |
| (未知) | `o0.U` | 服务器Base URL配置 |

### 2.5 NCUID 存储 (v1.4.x新增, jadx 确认)

| 键名 | 类型 | 说明 |
|---|---|---|
| `my_ncuid` | String | 当前用户NCUID (LoginActivity存储) |
| `direct_ncuid_*` | String | 私聊NCUID前缀存储 (v1.4.x新增) |
| `friend_ncuid` | String | 好友NCUID (ChatSettingsActivity等) |
| `to_ncuid` | String | 消息接收方NCUID (ChatListActivity等) |

### 2.6 音乐相关偏好 (v1.4.x新增)

| 键名 | 类型 | 说明 |
|---|---|---|
| `music_dynamic_bg_enabled` | boolean | 动态背景开关 |
| `music_lyric_bounce_enabled` | boolean | 歌词弹跳效果开关 |
| `music_lyric_color_mode` | String/int | 歌词颜色模式 |
| `music_bg_style` | String/int | 背景样式 |

---

## 3. 数据库系统

### 3.1 初始化

在 `OldChatApplication.onCreate()` 中:
```java
L.c(this);  // 数据库初始化
```

### 3.2 推测的数据库表

| 表 | 推测内容 | 关联Activity |
|---|---|---|
| messages | 聊天消息记录 | ChatActivity, GroupChatActivity |
| conversations | 会话列表 | MainActivity (聊天Tab) |
| contacts | 好友列表 | AddFriendActivity |
| groups | 群组信息 | GroupManageActivity |
| group_members | 群成员 | GroupMembersActivity |
| moments | 动态/朋友圈 | MomentsActivity |
| moment_comments | 动态评论 | MomentCommentsActivity |
| music_items | 音乐条目 | MusicPlazaActivity |
| **music_playlists** | **播放列表** | **PlaylistDetailActivity (v1.4.x新增)** |
| resources | 资源条目 | ResourceSectionsActivity |
| favorites | 收藏 | FavoritesActivity |
| notifications | 通知 | NotificationChatActivity |
| oldview_history | B站观看历史 | OldViewHistoryActivity |
| oldview_favorites | B站收藏 | OldViewFavoritesActivity |

---

## 4. 文件存储

### 4.1 音乐缓存

| 属性 | 值 |
|---|---|
| 路径 | `{filesDir}/music_player_cache/` |
| 命名 | SHA1(url) + 扩展名 |
| 上限 | 220MB / 120个文件 |
| 清理 | LRU策略，后台线程自动清理 |
| 支持格式 | .mp3, .m4a, .aac, .amr, .3gp, .wav, .ogg, .flac |

### 4.2 证书文件 (v1.4.x新增)

| 文件 | 说明 |
|---|---|
| 9u.pem | 证书文件 (jadx确认新增) |
| org/conscrypt/conscrypt.properties | Conscrypt TLS配置 |

### 4.3 文件提供者 (FileProvider)

```xml
<provider
    android:name="android.support.v4.content.FileProvider"
    android:authorities="com.im.oldchat.fileprovider"
    android:exported="false"
    android:grantUriPermissions="true">
    <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/file_paths"/>
</provider>
```

---

## 5. 数据模型

### 5.1 B站数据模型 (v1.4.x大幅扩展, jadx 确认)

v1.4.x 对 B站数据模型进行了大幅扩展，`BiliModels.java` 包含 40+ 子类:

#### 通用响应
```java
BiliResponse<T> {
    int code;       // 0=成功
    T data;         // 数据
    String message; // 消息
    int ttl;
}
```

#### 新增模型类 (v1.4.x)

| 模型类 | 说明 |
|---|---|
| BiliCookie | Cookie信息 (name, value, expires, httpOnly, secure) |
| CookieInfo | Cookie详情 |
| NavData / NavResult | 导航数据 |
| TokenInfo | Token信息 |
| QRAuthCodeResult / Response | QR码认证结果 (url, auth_code) |
| QRPollResult / Response | QR码轮询结果 |
| SearchResult / SearchData / SearchItem | 搜索结果 |
| VideoPage | 视频分P |
| VideoStat | 视频统计 |
| VideoOwner | 视频UP主 |
| FavFolder / FavFolderData / FavFolderResult | 收藏夹 |
| FavMedia / FavUpper | 收藏媒体/UP主 |
| FavResourceData / FavResourceResult | 收藏资源 |
| HistoryData / HistoryItem / HistoryResult | 观看历史 |
| HistoryCursor / HistoryDetail | 历史游标/详情 |
| PlayUrlData / PlayUrlDurl / PlayUrlResult | 播放地址 |
| RecommendData / RecommendItem / RecommendResult / RecommendArgs | 推荐 |
| CommentData / CommentPage / CommentContent / CommentMember | 评论 |
| CommentResult / CommentTop / CommentReply / CommentUpper | 评论详情 |
| DescButton | 描述按钮 |
| SimpleResult | 简单结果 |
| VideoDetailData / VideoDetailResult | 视频详情 |

### 5.2 数据注解

应用使用自定义注解 `@A.c` 进行JSON字段映射:
```java
@A.c("http_only") public int httpOnly;
```

---

## 6. 缓存策略

### 6.1 内存缓存

- 全局 Context: `OldChatApplication.f2156a`
- 连接状态: `g0.j` 单例
- 上传状态: `ResourceUploadService.f2313h` (静态)

### 6.2 磁盘缓存

| 缓存类型 | 位置 | 策略 |
|---|---|---|
| 音乐缓存 | filesDir/music_player_cache/ | LRU, 220MB上限 |
| 图片缓存 | (推测在cacheDir) | 系统自动管理 |
| 数据库 | databases/ | 应用管理 |
| B站认证 | BiliAuthStore管理 | 持久化 |
| NCUID存储 | SharedPreferences | 直接KV存储 |

---

## 7. 数据安全

### 7.1 明文存储风险

| 数据 | 存储方式 | 风险等级 |
|---|---|---|
| access_token | SharedPreferences (明文) | 中 |
| refresh_token | SharedPreferences (明文) | 中 |
| saved_username | SharedPreferences (明文) | 低 |
| saved_password | SharedPreferences (**明文**) | **高** |
| user_id | SharedPreferences (明文) | 低 |
| my_ncuid | SharedPreferences (明文) | 低 |

### 7.2 备份风险

`allowBackup=true` 意味着通过 ADB 可以提取所有 SharedPreferences 和数据库文件。

### 7.3 建议

- 密码应使用加密存储 (如 EncryptedSharedPreferences)
- Token 应存储在 Android Keystore 加密的容器中
- 应设置 `allowBackup=false` 或排除敏感文件
