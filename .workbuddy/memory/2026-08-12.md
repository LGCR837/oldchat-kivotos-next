# 2026-08-12 工作日志

## 频道头像 401 根因与修复（关键！）
- **现象**：`/channel-media/ca-XXX.jpg` 无论 v1 Bearer / v2 签名 / 无鉴权 都 401，控制台刷 ~300 条。
- **真正根因**：`resolveMediaUrl` 把 `channel-private:ca-XXX.jpg` 映射成 `http://oc.mcl0.dpdns.org/channel-media/ca-XXX.jpg` 形态本身**正确**（文档 §14.10：`GET /channel-media/{filename}` 是「全局签名下载」，签名在文件名里）。问题在 `shouldCache()` 对 `http://oc.mcl0/channel-media/...` 返回 true → 频道头像被 `MediaCache` 接管，经 `fetchOne` 带了 `Authorization: Bearer`（及 v2 签名头）去抓**签名 URL** → 服务端对带鉴权头的签名 URL 直接拒签返回 401。且 MediaCache 的异步多阶段管线（memCache 未命中→IDB 查找→fetchAndStore→fetchOne→tauriHttpFetch/reqwest 往返 + 重试循环）把请求推到签名 TTL 之后 → 一旦过期永远重试 → 刷屏。
- **修法**：`shouldCache()` 对 `/channel-media/` 与 `channel-private:` 返回 false。`MediaCache` 不再接管频道媒体 → `<img>` 保留解析后的签名 URL，由**浏览器原生发无鉴权 GET**（与官方 Android 客户端 ImageView 一致）。`channelCard` 自带 `onerror="this.src='assets/default-avatar.png'"` 优雅回落。
- **效果**：消除 401 刷屏；签名在 TTL 内（发现页实时 discover 返回的签名新鲜）即出图；过期则回落默认头像而非裂图/刷屏。
- **副作用**：`fetchOne` 里的 `signV2ForAnyPath` 频道媒体分支变为死代码（不再被调用），保留无害。
- **验证**：`node --check src/app.js` 通过。前端改动热重载即可，无需重编 Rust。用户需 `npm run tauri dev` 重启看发现页频道头像。
- **残余风险**：侧边栏「我的频道」用 localStorage `oc_subscribed_channels` 缓存的 `avatar_url`（跨设备订阅同步时写入），若该缓存签名已过期 → 回落默认头像（不再刷屏，但可能不出图）。根治需订阅列表也走实时元数据刷新（待做）。

## 关键结论（频道媒体鉴权模型）
- 频道**头像/封面/频道媒体** = `channel-private:ca-XXX[.ext]` → `/channel-media/ca-XXX[.ext]`，**全局签名下载、URL 即凭证、严禁带 Authorization 头**，必须用原生 `<img>` 加载。
- 频道**帖子里的文件媒体** = `https://files.mcl0.dpdns.org/v2/files/download/xxx`（文档 §14.7 示例），走 `/v2/files/download/{id}`，仅 Bearer 鉴权（§4.5），可走 MediaCache。
- 两者不要混：之前把频道媒体错当 `/v2/files/download` 是 404 源头；后来改 `/channel-media/` 路径对了但被 MediaCache 带鉴权头搞 401。

## ⚠️ 用户于 2026-08-12 00:13 明确「算了吧」放弃频道头像修复
- 经多轮（删 XHR 兜底 / 加 Rust fetch_media 命令 / 改 resolveMediaUrl 路径 / 试 v1+v2 鉴权 / shouldCache 跳过）仍不出图。根因大概率是：后端 `discover` 返回的 `ca-XXX` 签名 URL **TTL 极短或签名格式客户端无法复现**，纯前端无法根治；且 `shouldCache=false` 后即便浏览器原生 `<img>` 加载也仍 401（用户实测仍不行）。
- **现状代码状态**：`shouldCache` 已跳过 `/channel-media/`（至少不再刷屏，无害）；`fetch_media` Rust 命令 + `signV2ForAnyPath` 现为**未被调用的死代码**。
- **不要重新开启此问题的排查**，除非用户主动要求。若日后要彻底解决，方向应是：让订阅/发现列表走实时元数据刷新、或后端提供不过期/长时效签名、或确认签名 URL 是否需要配合 Referer/Cookie。客户端侧已无更多可试姿势。

## 新增：蔚蓝档案点击特效（ba-click-fx，方案 A 本地 vendored）
- **来源**：https://github.com/CialloKing/ba-click-fx —— 蔚蓝档案点击/拖尾特效，纯 JS 零依赖，默认 WebGL2、可 WebGPU、弱 GPU 回退 Canvas2D/软件 Bloom。全屏覆盖层 `pointer-events:none`、zIndex 2147483647、`mix-blend-mode:plus-lighter`（已读源码确认不挡点击）。
- **接入方式**：下载 IIFE 构建（~551KB）到 `src/vendor/ba-click-fx.iife.js`，`index.html` 在 `app.js` 之前 `<script src="vendor/ba-click-fx.iife.js">`（与 dumogu-scrollbar/artplayer/fengari 同款本地 vendored 模式）。全局名 `BAClickFX`，构造在 `BAClickFX.BAClickFX`，无参即自动全屏+绑定全局 pointerdown。
- **开关**：设置 → 通用 → 新增「蔚蓝档案点击效果」`oc-switch` 开关，存 `localStorage oc_click_fx`，**默认开启**（缺省≠'0' 即开）。
- **实时生效逻辑**：`applyClickFx()` 读开关 → 开且无实例则 `new BAClickFX.BAClickFX()`；关则 `window.__baFx.destroy()` 并置 null（关掉即真关掉，不残留覆盖层/监听）。`DOMContentLoaded` 收尾调用一次 `applyClickFx()`，设置变更即时调用。
- **验证**：`npm run tauri dev`（仅需前端重载，无 Rust 改动）。点击应出现 BA 波纹；关开关后波纹消失。
- **未 commit**（用户未要求）；若后续提交，属于独立功能提交。

## 更新：ba-click-fx 设置迁移 + 详细配置弹窗（2026-08-12 晚）
- **迁移**：「蔚蓝档案点击效果」开关从 设置→通用 移到 设置→主题，与「深色模式」「连消息」合并进**同一个 settings-group 块**（不再分开展示）；「连消息」的解释文本已移除（仅留开关本身）。
- **详细效果配置弹窗**：开启特效后，该块下方出现「详细效果配置」入口（id=`baClickFxDetailEntry`，默认 `display:none`，开关开启才显示）；点击打开**带进入/退出动画**的弹窗（复用 `.custom-modal-overlay`/`.custom-modal`，新增 `.closing` + `modalFadeOut`/`modalSlideOut` 退出动画，animationend 后移除 DOM）。
- **弹窗内容**：主题色 `<input type=color>`、11 个滑块（opacity / scale / bloom.clickEmissionScale / bloom.trailCoverageScale / shards.clickCount / shards.maxCount / shards.trailSpacing / rings.lifetimeMs / trail.lifetimeMs / trail.width / maxDpr）、2 个开关（clickEnabled / trailEnabled）。改动即时写 `localStorage oc_click_fx_params` 并 `rebuildClickFx()` 重建实例生效。
- **默认参数（用户指定）**：`getClickFxParams()` 强制 `opacity=0.85`、`bloom.clickEmissionScale=0.65`，其余走库原生默认（不写入 params）。
- **关键函数**：`getClickFxParams()`（读 params+合并默认）、`rebuildClickFx()`（destroy+applyClickFx）、`openBaFxModal()`（防重复、点空白/✕ 关闭带退出动画）。
- **验证**：`node --check src/app.js` 通过。需 `npm run tauri dev` 重启实测：主题 tab 合并块、详细入口显隐、弹窗动画与滑块即时生效。
- **仍未 commit**（用户未要求）。

## 更新：ba-click-fx 三处微调 + 聊天区禁选中（2026-08-12 凌晨）
- **1) 配置页面去滚动条**：`.settings-content` 加 `scrollbar-width:none` / `-ms-overflow-style:none` / `::-webkit-scrollbar{display:none}`（保留滚轮滚动，仅隐藏条）。与项目既有隐藏滚动条写法一致。
- **2) 聊天区非文本区域禁选中**：新增 `.chat-area{user-select:none}`，并对 `.message-bubble`、`.input-area textarea/input` 重新 `user-select:text`，使消息文本与输入框仍可选中/复制，头像/名称/时间/工具栏/按钮不再误选。沿用 3002 处既有 user-select 写法。
- **3) 详细配置新增「左键触发/右键触发」**：库默认 `_acceptPointerDown` 拒绝所有非左键(`e.button>0`)，所以原仅左键触发。新增 `leftClick`/`rightClick` 两开关（默认都 true），存进 `oc_click_fx_params`；`applyClickFx()` 建实例后调用 `installClickFxButtonGate()` 覆盖实例 `_acceptPointerDown`，按开关放行左键(button0)/右键(button2)，每帧读 `getClickFxParams()` 故切换即时生效、无需重建。弹窗 TOGGLES 数组追加这两项。
- **验证**：`node --check src/app.js` 通过。**仍未 commit**（用户未要求）。

## 更新：ba-click-fx 详细配置弹窗再调整（2026-08-12 01:1x）
- **去掉底部提示语**：删除「调整即时生效。点击空白处或 ✕ 关闭。」那行文字。
- **新增「恢复默认」「确认」按钮**：弹窗底部改为两按钮。`确认`= 关闭弹窗（保留原有的即时生效逻辑：滑块/开关 change 即写 `oc_click_fx_params` 并 `rebuildClickFx()`）；`恢复默认`= 清空 localStorage `oc_click_fx_params` → 回到库原生默认+我们指定默认（opacity0.85/bloom.clickEmissionScale0.65/leftClick&rightClick true），即时生效并同步所有滑块/开关/主题色控件位置。
- **弹窗滚动条去掉**：卡片加 class `ba-fx-modal`，`.ba-fx-modal` 在 app.css 隐藏滚动条（仍滚轮可滚）。
- **「详细效果配置」入口去粉色**：主题设置块里的入口 label 去掉 `color:var(--accent)`，chevron 改为 `var(--secondary-text)`（与普通设置项一致）。
- **验证**：`node --check src/app.js` 通过。**仍未 commit**。

## 提交：ba-click-fx 整段改动（2026-08-12 01:2x）
- 用户要求先 commit push。因 origin/main 本地跟踪关系丢失（`[gone]`），但远程 `origin/main` 仍在（528ccb0），直接 `git commit` 后 `git push -u origin main` 重建跟踪。
- 提交 `b446c71`：`Feat: 集成 ba-click-fx 鼠标指针特效与设置面板`，含 `src/vendor/ba-click-fx.iife.js`（新建）、`src/app.js`/`src/app.css`/`src/index.html`、记忆日志。5 files changed, 1826 insertions。

## 更新：聊天选中边界约束（2026-08-12 01:2x，未 commit）
- **需求**：聊天界面允许选中昵称(`.message-sender`)和文本(`.message-bubble`)，但选中**不要跨界**——限制在单个消息气泡内、或单个昵称内，不允许拖选跨到相邻消息，也不允许昵称↔文本互穿。
- **实现**：
  - CSS：`.chat-area .message-sender, .chat-area .message-bubble { -webkit-user-select:contain; user-select:contain }`；`.chat-area`（容器）仍 `user-select:none`（头像/时间/工具栏不可选）。`contain` 在 Chromium/WebView2 原生把选中钳制在元素边界内。
  - JS 兜底 `setupSelectionBoundary()`（DOMContentLoaded 调用）：`mousedown`(capture) 记录起点单元（气泡或昵称），`selectionchange` 时若实时选区终点跨越到不同单元，则用 `Range.comparePoint` 把焦点夹回起点单元边界。为不支持 `contain` 的引擎（如 Linux WebKitGTK）保证不跨界。textarea/input 走 `user-select:text` 且 mousedown 命中它们时 originUnit=null，不受影响。
- **解释点**：当前 impl 把「气泡」与「昵称」各自作为独立单元（最严格「不跨界」），即同一条消息的昵称+气泡也不能一起选中。若用户其实想要「同一条消息的昵称+气泡可一起选中、但不可跨消息」，需改为以 `.message` 为单元——待用户反馈。
- **验证**：`node --check src/app.js` 通过。**未 commit**（用户未要求）。

## ⚠️ 修正：聊天选中全失效（2026-08-12 01:2x）
- **现象**：用户实测「啥都没法选了，一个选择框都看不到」。
- **根因**：用 `user-select: contain` 做跨界约束，但运行环境（WebView2）未生效、被当作 `none`，导致 `.message-bubble`/`.message-sender` 继承容器 `.chat-area` 的 `none` → 整片聊天不可选。
- **修法**：弃用 `contain`。气泡/昵称/输入框统一改回 universally 支持的 `user-select: text`（保证一定可选中）；跨界约束**完全交给 JS** `setupSelectionBoundary()`：移除 mousedown 记录起点的写法，改为 `selectionchange` 时从实时选区的 `startContainer` 推断起点单元、若 `endContainer` 落在不同单元则用 `Range.comparePoint` 把终点夹回起点单元边界。全部引擎通用，不再依赖 `contain`。
- **验证**：`node --check` 通过。**未 commit**。

## 侧边栏：频道默认折叠 + 私聊支持置顶/折叠（2026-08-12 01:3x，未 commit）
- **频道默认折叠**：`_loadCollapsedState()`（app.js ~6075）默认值新增 `if (!('channel' in s)) s.channel = true;`，使「频道」分区(`data-section="channel"`)默认收起。已显式展开过(存了 `channel:false`)的用户不被覆盖，仅对未存储该键的新/旧用户生效。折叠态持久化键 `oc_section_collapsed`，点击分区标题 `onSectionHeaderClick` 切换并保存。
- **私聊支持置顶/折叠**：右键菜单 chat 面板原来只有 `group`/`channel` 分支带 pin/fold，`else`(direct 私聊) 仅有「全部已读」。现已在 direct 分支补上「置顶/取消置顶」「折叠/取消折叠」菜单项（与 group/channel 同款 `isPinned/isFolded` 文案）。后端逻辑 `togglePinned/toggleFolded(convKey)` 与 `applyPriority`/`layoutFoldSection` 本就按 `convKey`(= `type:id`) 通用处理 direct（确认 `createContactItem` 给 direct 项设了 `dataset.convKey='direct:<id>'`），无需改渲染，加菜单即生效。
- **验证**：`node --check src/app.js` 通过。需 `npm run tauri dev` 实测：频道区默认收起、私聊右键出现置顶/折叠且功能生效。**未 commit**。

## 侧边栏置顶左条：仅 hover 显示 + 高度 0→满 非线性动画（2026-08-12 01:4x，未 commit）
- **改动**：原 `.contact-item.is-pinned { box-shadow: inset 3px 0 0 0 var(--accent) }` 常驻显示 → 改为伪元素 `::before` 左条（left:0/top:0/bottom:0/width:3px/background:var(--accent)），`transform: scaleY(0)` 默认收起，仅 `.contact-item.is-pinned:hover::before` 时 `scaleY(1)`。视觉上等价「高度从 0 到满」，用 transform 走 GPU 合成（比动画 height 更顺）。
- **非线性缓动**：显示 `transition: transform .36s cubic-bezier(0.22,1,0.36,1)`（弹性展开）；隐藏 `transition: transform .24s cubic-bezier(0.4,0,0.2,1)`（略快平滑收起）。未用 opacity 渐变，避免土味。`.contact-item` 既有 `position:relative`（270 行）可直接定位伪元素。
- **验证**：纯 CSS 改动，无 JS 依赖（`is-pinned` 仍由 app.js:6275 切换）；未 commit。

## 默认主题深色模式：messagesContainer 左上角同色小圆角补丁（2026-08-12 01:4x，未 commit）
- **需求**：默认主题 + 深色模式下，在 messagesContainer 左上角加一块与标题栏同色的「小圆角」，平滑转角；侧边栏收起则不显示；浅色模式与其他主题不变。
- **根因几何**：深色默认主题 `--header-bg:#16213e`（标题栏），消息区透明透出 body `--bg:#1a1a2e`，二者在 messagesContainer 左上直角处有色差硬接缝；侧边栏收起时 chat-area marginLeft 变 0（app.js:9433），该角恰落窗口左上圆角处。
- **实现**：
  - JS：`applyThemeById` 注入非 default 主题后给 `documentElement` 打 `data-custom-theme=<id>` 标记，default/无 css 则 `removeAttribute`。用于在 CSS 稳健区分「默认主题 vs 其他主题」（避开 `:has` 兼容风险）。
  - CSS：`app.css` 在 `.messages` 后新增 — 选择器 `:root:not([data-custom-theme])[data-theme-mode="dark"] .chat-area::before`：`position:absolute; top:var(--header-height); left:0; 14x14; background:var(--header-bg); border-top-left-radius:14px; pointer-events:none; z-index:5`（挂在 .chat-area，非滚动、被 overflow:hidden 裁切在角内）。再用 `.sidebar.collapsed ~ .chat-area::before { display:none }` 实现侧边栏收起隐藏。
- **验证**：`node --check` 通过；纯视觉改动，未 commit。

## 重点分区：私聊自动进入 + 默认进入0s + 移除30s滑块 + 改名（2026-08-12 02:1x，未 commit）
- **根因（私聊不进重点）**：`openConversation`（app.js ~7060）原来仅 `if (type === 'group')` 挂「进入延迟定时器」→ `markPrioActivity(convKey)` 把群聊拉入重点；私聊分支走不到，只能靠未读进入。群聊正常、私聊不行即源于此。
- **改动 1（自动进入）**：条件改为 `if (type === 'group' || type === 'direct')`，私聊打开后也经 `getPrioEnterDelay()` 定时器（现默认 0s）立即进入重点，与群聊一致。
- **改动 2（默认进入0s）**：`getPrioEnterDelay()` 默认分支由 `return Infinity`(=31 从不) 改为 `return 0`（立即进入）；历史存储的 31 也按 0 处理。
- **改动 3（移除30s）**：删除设置里「进入延迟」滑块整行（`settingsPrioEnter`/`prioEnterInput`/`prioEnterVal` 及对应 `bindPrioTimeSlider` 调用），保留「闲置移除」滑块（0~31，默认 0）。
- **改动 4（改名）**：设置标签「重点分区」→「自动重点分区」；说明文字同步（默认关闭、进入 0 秒、群聊与私聊均自动进入）。
- **默认不启用**：「自动重点分区」开关本身默认即关（`isPriorityEnabled()` 读 `oc_priority_section==='1'`，未设置=false），符合需求，无需额外改动。
- **验证**：`node --check` 通过；未 commit。

## 侧边栏分区拖拽排序（2026-08-12 晚）
- 需求：拖动分区标题重排整个分区；拖动中全部收起仅显示标题，拖完恢复；顺序持久化。
- 实现：app.js 新增 `SECTION_ORDER_LS_KEY=oc_section_order` + `_load/_save/_applySavedSectionOrder`（renderContacts 末调用还原顺序）；`onSectionHeaderPointerDown`(pointerdown 委托) → 阈值 6px 区分点击/拖拽 → `_beginSectionDrag` 给 contactList 加 `reordering-sections` 并强制所有 `.contact-section` 收起(maxHeight=headerH)；`onSectionPointerMove` 实时按指针 Y 与各分区中线比较做 live insertBefore 重排；`onSectionPointerUp` 抑制附带 click、恢复 `applySectionCollapsed(false)`、`_saveSectionOrder()` + `schedulePriorityApply()`。
- 防冲突：`onSectionHeaderClick` 首行检查 `_suppressSectionClick`；`applyPriority` 首行遇 `reordering-sections` 直接 return（拖拽中不重置收起态）；`onSectionHeaderPointerDown` 首行清残留抑制标记。
- CSS：`app.css` 加 `.contact-section.section-dragging`(z-index/高亮标题/box-shadow/cursor:grabbing) 与 `.reordering-sections .contact-section-header{cursor:grabbing}`。
- **验证**：`node --check` 通过；未 commit。

## 重点分区设置项恢复 + 默认参数调整（2026-08-12 晚，接续上一条）
- 用户纠偏：之前误把"移除30s"理解为"删掉进入延迟滑块"，实际只要求改默认值、保留配置项。已恢复。
- 恢复「进入延迟」滑块（settingsPrioEnter / prioEnterInput / prioEnterVal），与「闲置移除」并列；开启重点分区时二者显示（prioSliderRows 含两项）。
- 默认值改为：进入延迟 = 5 秒（getPrioEnterDelay 默认 return 5*1000，保留 0=立即/1~30=秒/31=不自动进入(Infinity) 语义）；闲置移除 = 30 秒（getPrioActiveMs 默认 return 30*1000）。
- 滑块范围 0~31，文案：进入延迟 0=立即/31=不自动进入；闲置移除 0=立即/31=永不移除。说明文字已同步。
- 标签仍为「自动重点分区」。openConversation 进入定时器逻辑未动（已支持 delay!==Infinity 跳过）。
- **验证**：`node --check` 通过；未 commit。

## 分区拖拽排序改版：transform 平滑位移 + 跟随鼠标（2026-08-12 深夜）
- 用户新要求：拖拽中收起动画用**原本** max-height 过渡（不瞬切）；标题**全部保持原样式**（去除 section-dragging 粉色高亮）；被拖者**连续跟随鼠标**；其他分区**自动上/下平滑平移**出空位。
- 重写 `_beginSectionDrag`/`onSectionPointerMove`/`onSectionPointerUp` + 新增 `_computeSectionFinalOrder(drag,dy)`：
  - 起手：记录各分区拖前 collapsed 态；给所有 `.contact-section` 加 `collapsed` + `transition:max-height .3s ease` + `maxHeight=headerH`（复用原折叠动画）；被拖者加 `drag-lifted`(仅 z-index:30，不改色) + `transition:max-height .3s ease`（transform 不加过渡，1:1 跟手）。
  - move：`drag.sec.style.transform=translateY(dy)` 跟随鼠标；用相对坐标(去掉 baseTop 常量)按各分区中点算 newIndex → `finalOrder`，其余分区 `transform:translateY((targetIndex-i)*dragH)`（dragH=折叠后高，等高等距）+ CSS `transition:transform .25s cubic-bezier(.22,1,.36,1)` 平滑移位。
  - up：提交 DOM 顺序(finalOrder appendChild 序列)；其他分区自然位==视觉位直接清样式无跳动；被拖者从鼠标位置 `translateY(landDy)` 经 rAF 清 transform 平滑落入槽位(transitionend/320ms 兜底)，再恢复 collapsed 态。
  - 数学保证：visualTop=homeTop+shift == 提交后新自然位，故其他项零跳动；仅被拖项落位动画。
- CSS：删 `.section-dragging` 粉色块；改 `.contact-section.drag-lifted{position:relative;z-index:30}`、`.reordering-sections .contact-section{transition:max-height .3s ease,transform .25s cubic-bezier(.22,1,.36,1)}`、`.reordering-sections .contact-section.drag-lifted{transition:max-height .3s ease}`、`.reordering-sections{cursor:grabbing}`。
- 防冲突不变：`onSectionHeaderClick` 查 `_suppressSectionClick`；`applyPriority` 遇 `reordering-sections` return；`onSectionPointerUp` 抑制附带 click。
- **验证**：`node --check` 通过；无 section-dragging 残留引用；未 commit。

### ⚠️ 拖拽两个 bug 的踩坑修正（同夜）
- Bug1 收起无动画：原手写 `s.style.maxHeight=headerH` 是从 `none/auto`→px，**CSS 无法对 none 插值**，故不动画。必须用 `setSectionCollapsed(s,true,true)`（先 scrollHeight 再 headerH+强制回流，才可插值）。
- Bug2 其他分区瞬移：起手循环里给每个分区内联 `s.style.transition='max-height .3s ease'`，**内联覆盖掉了** `.reordering-sections .contact-section` 的 `transform .25s` 过渡 → transform 无过渡即瞬移。修正：others 不写内联 transition（交给 CSS），仅被拖者内联 `transition:max-height .3s ease`（去掉 transform 过渡以 1:1 跟手）。
- 结论：拖拽排序里"复用原折叠动画"与"transform 平滑移位"要分别走 `setSectionCollapsed(...,true)` 与 CSS transform 过渡，绝不能手写内联 transition 把 transform 过渡抹掉。

## 侧边栏卡片左条 + 置顶三角（2026-08-12 深夜）
- 需求：把置顶卡片左侧那条 hover 高亮竖条(scaleY 居中非线性动画)应用到**全部** `.contact-item`；置顶卡片额外在左上角加**小三角**标记。
- 改 `app.css`：`.contact-item.is-pinned::before` 改为 `.contact-item::before`（全部卡片 hover 显示同款竖条，动画不变）；新增 `.contact-item.is-pinned::after` 用 border 三角(`border-width:11px 11px 0 0; border-color:var(--accent) transparent transparent transparent`) 置于 `top:0;left:0` 作左上角置顶标记。`.contact-item` 本身已有 `position:relative`，无需再加。
- **验证**：纯 CSS；未 commit。

## 侧边栏竖线开关 + ba-click-fx 默认关闭（2026-08-12 凌晨，已 commit 4ed7733 后追加、待下次提交）
- **ba-click-fx 默认关闭**：`isBaClickFxEnabled()` 由 `!== '0'`(默认开) 改为 `=== '1'`(默认关，需显式开启)。设置页「蔚蓝档案点击效果」开关 `checked` 仍读该函数，故默认 OFF；老用户已存 '1' 的保持开启。注释同步更新。
- **新增两个设置开关（设置→主题 同块，在 ba-click-fx 之后）**：
  - 「侧边栏竖线」`sidebarBarToggle` → 存 `oc_sidebar_bar`，默认开（`!== '0'`）。关 → `body.sidebar-bar-off`，CSS `body.sidebar-bar-off .contact-item::before{display:none}` 隐藏全部竖条（hover 与选中常驻都无）。
  - 「侧边栏选中竖线常驻」`sidebarActiveBarToggle` → 存 `oc_sidebar_active_bar`，默认开。关 → 移除 `body.sidebar-active-bar`，则 `.contact-item.active::before` 不再强制 `scaleY(1)`，选中项仅 hover 时显示竖条（鼠标移开即无）。
- **CSS 改动**：原 `.contact-item.active::before{transform:scaleY(1)}` 改为 `body.sidebar-active-bar .contact-item.active::before{transform:scaleY(1)}`（受开关控制）；新增 `body.sidebar-bar-off .contact-item::before{display:none}`。
- **JS 启动**：DOMContentLoaded 收尾新增 `applySidebarBarSettings()`（读两键→toggle `sidebar-bar-off`/`sidebar-active-bar` 两个 body class），设置页两开关 change 即调用即时生效。
- **用户偏好提醒**：用户对本轮多次强调「按描述严格实现、不乱改位置/形状」。置顶三角曾因两道 border 着色误渲染成正方形（底边/左边为0时两三角拼满方块），最终用「仅右边着色 + border-width:0 8px 8px 0」做出直角在右上角、斜边朝内的折角三角。
- **验证**：`node --check src/app.js` 通过。

## 公开法庭（public-court）官方接口分析（2026-08-12 下午，仅分析未改）
- **官方契约**（mcl0-new-docs §17.3，与 mcl0-old-docs routes.md 完全一致，8 端点）：
  - `GET /v1/public-court/cases` 列表 / `GET /v1/public-court/cases/{caseID}` 详情
  - `GET /v1/public-court/cases/{caseID}/votes` 投票结果 / `GET /v1/public-court/cases/{caseID}/discussions` 讨论（**两个独立 GET 端点**）
  - `POST .../vote` {vote,reason,evidence} / `POST .../statement` 陈述 / `POST .../discussion` 讨论 / `POST .../withdraw` 撤销
  - §17.3 标注「/v1 独有」→ 无 v2 版本，必须走 v1。
- **文档缺口**：新旧文档都**只有端点名、无任何响应字段示例**；线上接口需登录（curl 直探返回 `unauthorized`）。真实字段名（尤其 vote 取值枚举、各 item 的 user/文本字段）只能靠真实样本确认。
- **当前代码 vs 官方 的差距**（app.js:11873-12055）：
  1. votes/discussions 是独立端点，但 renderCourtCase 从**案件详情内嵌数组**读 → 永远空 → "现有 0 票"恒为 0、讨论区恒空白（=「很多内容无法显示」主因）。
  2. statements 仅 POST 无 GET，若详情不内嵌则陈词也不显示。
  3. 投票 POST 发 `{}`，应按文档发 `{vote,reason,evidence}` → 当前投票大概率无效。
  4. `POST /withdraw` 完全未实现（缺撤销按钮）。
- **调试文本残留（待删）**：列表空时 `原始响应（无案件）` <details> dump；详情兜底 `未能识别案件字段，显示原始响应` <details> dump；两处 `console.error('[court] ...')`。
- **版本协商结论**：public-court 不在 V1_TO_V2 映射，mapToV2 原样返回 /v1 路径 → 默认 v2优先 模式也能正确走 v1（仅「仅v2」模式下会被判无 v2 版本报错，边缘情况）。
- **待确认**：实施修复前需真实响应样本。三种获取方式待用户选：①临时加「显示原始 JSON」开关让用户登录后复制；②用户直接粘贴样本；③用户提供登录凭据脚本探接口。

## 公开法庭：加控制台调试（2026-08-12 下午，按用户选择「先全部加控制条调试信息」）
- 用户选「先加 console.warn 调试、以警告形式抛出方便分离」，而非直接改 UI。
- **新增统一前缀 `[COURT-DEBUG]` 的 console.warn**：
  - `GET /cases` 列表原始响应
  - `GET /cases/{id}` 详情（unwrap 后）原始响应
  - 详情内嵌 `statements` 解析结果
  - 新增并行拉取 `GET /cases/{id}/votes` 与 `/discussions`（之前完全没拉 → 这是「很多内容无法显示」主因），各 dump 原始响应
  - 三个写操作（vote/statement/discussion）的 **请求体 + 响应(status+json)**，便于看服务端对字段的反馈（尤其 vote 现在发 `{}` 是否被拒、期望什么 `vote` 枚举）
- **删除 UI 调试残留**：列表空时的「原始响应（无案件）」<details> dump、详情「未能识别案件字段」<details> dump 全部移除，改用正常文案（"暂无案件"/"该案件暂无更多内容"）；两处 console.error 改 console.warn。
- **投票/讨论现已真正拉取**：votes/discussions 改用 `/votes`、`/discussions` 端点数据（并兼容详情内嵌兜底），若字段名恰中 courtPick 别名则内容直接显示；主要目的仍是捕获真实 schema。
- 未 commit（debug-only，待用户从控制台取样本后据实修复字段名/补 withdraw/修 vote 体）。

## 公开法庭：文档契约核对 + 空列表根因（2026-08-12 晚）
- **用户实测**：手机端能显示「历史已锁定的法庭」，但桌面端裸 `GET /v1/public-court/cases` 返回 `{cases: Array(0)}`。
- **文档核对结论**：逆向文档（nx1/nx2/nx3 的 `02-网络层与API通信机制.md`）**完全没有 public-court 的 Retrofit 接口/字段 schema/查询参数**，只有 `PublicCourtActivity`/`PublicCourtCaseDetailActivity` 两个页面名；新官方文档 `mcl0-new-docs/api202608100558.md` §17.3 也只列 8 个端点名，**无任何参数与响应字段示例**；`mcl0-old-docs/api.md` 同样无 public-court。→ 契约在文档里确实没写全。
- **空列表根因假设**：列表端点默认只返「进行中/未锁定」案件（当前 0 条），手机端靠某个状态筛选参数才能拉到「已锁定/历史」案件。文档无法给出参数名。
- **临时探测 `[COURT-PROBE]`**（app.js loadCourtCases 空分支内）：基础请求为空时并行试 13 种常见状态参数（`status=all/closed/locked/open`、`state=all/closed/locked`、`include_closed=true`、`closed=true`、`include_locked=true`、`type=all`、`tab=all`、`filter=all`），各打 `count=N` + 原始响应。→ 用户刷新案件列表看哪条 count>0 即可定位真实参数。
- **结果（用户实测）**：仅 `status=all` 返回 `count=17`，其余 12 个全 0。结论：列表端点默认只返非-all 状态（≈仅进行中），`?status=all` 才会返回全部含已锁定/历史案件（=手机端能看历史锁定的原因）。
- **已修复**：列表请求改为 `GET /v1/public-court/cases?status=all`；移除 COURT-PROBE 探测块（已找到答案，不再发 13 请求）。detail/votes/discussions 的 COURT-DEBUG 保留，待用户点开案件取样本。
- 未 commit（debug-only，待取 detail/votes/discussions 样本后据实修字段名/补 withdraw/修 vote 体，再统一清 COURT-DEBUG 并 commit）。

## 公开法庭：详情响应结构 + title 兜底（2026-08-12 15:09）
- **用户实测样本（关键）**：点开案件 `NdTii-f-hdQlYtab` 后控制台输出：
  - 详情：`{ case: {...}, discussions: Array(2), merged_report_page:1, merged_report_page_size:5, merged_report_total:17, … }` → 详情是**`case` 包裹**，且 `discussions`/`merged_report_*` 在**顶层**（case 的兄弟）。
  - statements（detail 内）：`Array(10)` → statements 在 `case` 对象内。
  - votes 端点：`{votes: Array(10)}`。discussions 端点：`{discussions: Array(2)}`。
- **根因（"全部未命名案件"）**：旧 unwrap 只认 `c.data`，不认 `c.case` → 一直取外层对象 → courtPick 找不到 title。已修：先 `c.data` 再 `c.case` 两层 unwrap。
- **标题兜底改为 `案件 #<id>`**（与手机一致），createCourtCaseItem 与 renderCourtCase 两处都改；hasAny 改为不依赖 title。
- **调试日志升级为 `JSON.stringify(...,null,2)` 完整文本**（detail RAW、case object、statements、votes、discussions、列表前 2 项），方便用户直接复制粘贴完整 schema（console 折叠的 `{…}` 看不到字段名）。
- **"举报内容"未显示**：疑似在 `merged_report_*` / case 内某个 reports 字段，但 console 是折叠 `{…}` 看不到。需用户粘贴完整 JSON 才能定位字段名再渲染。用户澄清 Fiddler 仅作"实在没办法"的最后手段，不要求现在抓包。
- 未 commit（debug-only）。

## 公开法庭：据真实 schema 完整重写（2026-08-12 15:13，用户给完整样本后）
- **真实字段 schema**（用户贴出完整 JSON）：
  - 列表项 / `case`：`id`、`reporter_*(uid/name/avatar)`、`defendant_*(uid/name/avatar)`、`report_reason`、`report_evidence`、`defense_reason`、`defense_evidence`、`status`(pending_review)、`verdict`(ban)、`ban_hours`、`ban_vote_count`/`keep_vote_count`/`total_vote_count`、`my_vote`(ban/keep/'')、`created_at`/`updated_at`/`closed_at`。
  - 详情**顶层**（与 `case` 同级）：`merged_reports:[{report_id,reporter_*,reason,created_at}]`、`merged_report_total`、`statements:[{user_uid,name,avatar,role(jury/reporter),reason,evidence,created_at,updated_at}]`、`discussions:[{id,user_uid,name,avatar,body,created_at}]`。**关键**：statements 在顶层不在 case 内（之前取空的根因）。
  - `GET /votes` → `{votes:[{voter_*,vote('ban'|'keep'),reason,evidence,created_at}]}`；`GET /discussions` → `{discussions:[...]}`。
- **重写内容**（app.js + app.css，未 commit，按用户要求"改好前别commit"）：
  1. 新增 helper：`courtStatusLabel`/`courtVerdictLabel`(中文标签)、`courtFmtTime`(unix秒→本地时间)、`courtPersonHTML`(头像+名+角色徽章+内容+时间，头像走 resolveMediaUrl 复用全局 img 候选降级)。
  2. `createCourtCaseItem`：被告头像 + `案件 #id` + 举报人→被告 + 状态徽章 + 封/留票数。
  3. `renderCourtCase`：正确 unwrap `c.case` + 从 **rawDetail 顶层**取 merged_reports/statements/discussions；富渲染头部徽章、举报人/被告、举报证据(pre)、合并举报列表、投票情况+投票列表(封禁/保留)、陈词/陈述(role标签)、讨论。移除"未命名案件"兜底。
  4. 写操作：投票拆「赞成封禁/赞成保留」两按钮 POST `{vote,reason,evidence}`（按官方契约）；陈词 POST `{reason,evidence}`；讨论 POST `{body}`；新增「撤销」按钮 POST `/withdraw {}`（撤回前 confirm）。全部成功后刷新详情。
  5. 移除全部 `[COURT-DEBUG]`/`[COURT-PROBE]` 日志（保留普通 error 日志）。
- **验证**：`node --check src/app.js` 通过；grep 确认 0 处 COURT-DEBUG/PROBE 残留。待用户在 Tauri 内实测渲染与投票/陈词/讨论/撤销是否生效。

## 公开法庭侧边栏改版：双头像 + 标题居中 + 文本换行（2026-08-12 15:25，未 commit）
- **需求**：①侧边栏案件卡片显示**两个人头像**（举报人+被告，原仅被告）；②标题「案件 #XXX」**居中**；③其他文本（举报人/被告名、徽章、票数）**自动换行**（原 uid 用 `white-space:nowrap` 截断成省略号）。
- **JS（createCourtCaseItem）**：新增取 `reporter_avatar`；渲染结构由「单头像 wrap + 左对齐文本」改为「`.court-avatars` 双头像并排 + `.contact-text` 居中列」；去掉 uid 的 `.slice(0,80)` 截断，全文显示。
- **CSS**：`.court-case-item .contact-info` 改为 `flex-direction:column; align-items:center; text-align:center`；新增 `.court-avatars`（flex 居中、gap6）；`.court-mini-avatar` 缩到 30px；`.contact-text` 加 `width:100%; text-align:center`；`.court-case-item .name` 改 `display:block;text-align:center`；`.court-case-item .uid` 改 `white-space:normal; word-break:break-word; overflow-wrap:anywhere; line-height:1.45`；`.court-case-tags` 加 `justify-content:center`。
- **验证**：`node --check src/app.js` 通过。未 commit（延续"法庭改好前别 commit"）。

## 公开法庭三处 bug 修复（2026-08-12 15:30，未 commit）
- **1) court-pre 没有字体**：`.court-pre` 未设 `font-family`，`<pre>` 回退 UA 等宽字体。已加 `font-family: inherit`（跟随应用字体）。
- **2) 法庭三大金刚键（窗口 min/max/close）无法点击**：
  - ⚠️ **本人在上一轮错判为「滚动条 rail 命中遮挡」并改了 `.dumogu-scrollbar-rail` 的 `pointer-events`**；用户实测否定（"更本不是这个原因"）。**已回退该 pointer-events 改动**。
  - **真正根因（经全量比对确认）**：法庭与发现页的窗口键 HTML / 绑定 `bindWinControls` / `app-region` 规则 / 滚动容器 `flex:1` 布局**逐字一致**，静态无解。差异在**运行期**：法庭是发现页的子面板、**非默认激活**，`.court-detail` 的 flex 布局（`flex:1`，git 历史证明从未用过 `height:100%`，故不是 plaza 那次 `height:100%→flex:1` 的毛病）；而 dumogu 自绘滚动条 wrapper `.dumogu-scrollbar` 是 `position:absolute; inset:0; z-index:999; pointer-events:none`，竖向 rail（`pointer-events:auto`）按滚动容器实时包围盒定位。法庭面板在 `DOMContentLoaded` 挂载滚动条时自身不可见，dumogu 拿到的容器矩形算错，轨道被定位到面板顶部 = header 区域，8px 宽贴右边缘正好压住右上角三键 → 吞点击。**发现页是默认激活面板**，挂载时可见、矩形正确，轨道落 header 之下，所以正常。
  - **两层修法（均与之前被否的 pointer-events 改动不是一回事）**：
    ① `app.css` `.chat-header` 加 `position:relative; z-index:1001`（高于 wrapper 的 999）——即使轨道偶尔算错，header 及三键始终在滚动条层之上可点（overlay 滚动条标准抬层写法，不破坏拖拽）。
    ② `app.js` `switchTab` 激活面板后用 `requestAnimationFrame` 调 `sb.update()` 重算子滚动条几何（面板可见、布局就绪后轨道才会被正确挪到 header 之下）。配合本会话早先为"最大化错位"加的 `window resize → sb.update()`，彻底覆盖"挂载错算 + 尺寸变化"两类几何失准。
  - **验证**：`node --check src/app.js` 通过。待 `npm run tauri dev` 实测：右上角三键可点、空态/有内容态最大化后滚动条对齐。
  - **教训**：① 同架构面板一个坏一个好，优先查"非默认/动态挂载"带来的运行期差异，而非静态结构；② 用户否定过的改法立刻回退，别恋战；③ 用户说"之前所有界面都遇到过、当时不是这么修的"→ 提示有统一既有修法（overlay 滚动条抬层 + 几何刷新），先核对模式。
- **3) 用命令强制最大化后滚动条错位**：`initSubScrollbar` 只靠 MutationObserver 刷新几何，窗口 resize（最大化/还原/拖边）不触发 mutation → 轨道不重算。修法：`app.js` 把四个子滚动条实例收进 `subScrollbars[]`，新增 `window resize` 监听（`requestAnimationFrame` 去抖）统一 `sb.update()` 重算几何。面板切换时的淡入 class 变动仍由既有 MutationObserver 覆盖。
- **验证**：`node --check src/app.js` 通过。待 `npm run tauri dev` 实测：court-pre 字体、右上角三键可点、最大化后滚动条对齐。未 commit（延续法庭改好前别 commit）。

## 法庭三键诊断模式修正（2026-08-12 16:25，未 commit）
- **问题**：用 `alert('[DIAG] 法庭三键区 pointerdown …')` 诊断后，**只弹了 pointerdown 一次，click 报警再也没弹** → 误以为"点击被吞"。
- **真正原因**：`alert()` 是**阻塞同步对话框**，会把原本的 mouseup 抢走（鼠标松开在 OK 按钮上而非原按钮），导致**紧随的 click 事件被取消**。所以 alert 弹完后我担心的"click 没到 invoke"其实是 alert 自己的副作用。
- **pointerdown 链路数据**（重要事实）：顶层命中 `i.fa-regular.fa-square`（按钮内 Font Awesome 图标）→ 完整链路 `i > BUTTON#courtWinMaxBtn > DIV#courtWinControls > DIV.header-right > DIV.chat-header > DIV.main-panel.active`——**点击其实到达了按钮**，且 `.main-panel` 已正确拿到 `.active`（`pointer-events:auto`）。这是上轮"非默认面板挂载时滚动条错位盖 header"假设的**反证**。
- **诊断模式改成非阻塞 toast**（关键经验）：
  - `src/app.js` 新增 `courtWinDiag()` IIFE，内含 `diagToast(title, lines)`：动态创建 `#courtWinDiagToast` div（`position:fixed; top:60px; left:50%; transform:translateX(-50%); z-index:99999; background:rgba(20,20,20,.92); color:#fff; pointer-events:none`），4 秒后自动隐藏。**`pointer-events:none` 是关键**——不阻断任何点击/拖拽。
  - click 钩子：三个按钮各挂 click → toast「法庭 click 到达」。
  - pointerdown 钩子：document 级 → 在 `#courtWinControls` 区域内时 toast「法庭 pointerdown + 顶层元素 + 链路」。
- **之后**：若 toast 显示 click 到达 → 按钮本身没问题；invoke 始终静默 → 排查 Rust 命令注册/错误吞掉。若 toast 完全不弹 → 改用 DOM 检查元素实际几何位置。
- **教训**：① `alert/confirm/prompt` 调试点击行为会**自毁测试**（吞 mouseup），要弹诊断必须用非阻塞 toast/console.log；② pointerdown 起到不代表 click 起到，反之亦然，两层都要独立看；③ 看到"拓扑链路对"就说明 z-index/pointer-events 这条路是通的，可以排除。

## 法庭三键根因收敛：invoke 结果被静默吞（2026-08-12 16:34，未 commit）
- **用户实测（非阻塞 toast 版）**：点法庭最大化键，弹了两条「法庭 click 到达 / courtWinMaxBtn」，**但无 invoke 结果**——因为当时那一版还没加 invoke 日志，且 `bindWinControls` 原 `.catch(function(){})` 把 invoke 错误静默吞掉，所以"点不动却无线索"。
- **关键事实复核**：① `invoke` 定义在 `initTauri()` IIFE 内（`const invoke = window.__TAURI__?.core?.invoke || window.__TAURI_INTERNALS__?.invoke`，app.js:15），`bindWinControls`(156) 与窗口键绑定同处该 IIFE，**闭包可访问 invoke**，非 undefined；② 主聊天键（app.js:145-153）用同一 `invoke` 调同一批命令（minimize/toggle_maximize/close_window）且**正常**，命令名与 `src-tauri/src/lib.rs:895-897` 注册完全对得上；③ 法庭主面板是 index.html 静态（252/258 行），非 JS 注入，`bindWinControls('courtWin')` 必能挂上监听。
- **最新诊断（用户要求改放控制台，toast 难复制）**：`diagInvoke` 现在在点击时**先 `console.log('[DIAG] >>> 准备 invoke: cmd=… typeof invoke=… invoke来源=core/internals/无')`**，再打 `<<< invoke OK/FAIL` 详情（FAIL 含 err.message），全部进控制台；toast 仅保留"click 到达"确认。DevTools 快捷键 Ctrl+Alt+Shift+F12（前端调 `toggle_devtools`，lib.rs:46）。
- **待用户实测**：开 DevTools 控制台 → 点法庭任一键 → 看是否有 `[DIAG] >>> 准备 invoke`（有=监听器触发、invoke 被调用；无=监听器没挂上，需查 bindWinControls 是否真绑定 courtWin）。据 console 的 OK/FAIL 文本定位下一步。

## ✅ 法庭三键根因定案 + 修复（2026-08-12 16:55，未 commit）
- **用户贴出控制台日志定案根因**：`[DIAG] bindWinControls(courtWin) => min=false max=false close=false`（绑定当时 court 三键 `getElementById` 返回 null，监听器全没挂上）；`[capture] click 进入 courtWinControls` 三次都触发（点击物理上到得了按钮，按钮没被遮挡）；但无任何 `[DIAG] click min/max/close courtWin`（监听器从未挂上故不触发）；`绑定后复查 => courtWinMinBtn=true`（绑定之后按钮才进 DOM）。musicWin 同样 `false`。结论：**不是按钮/遮挡/Rust 问题，`bindWinControls('courtWin'/'musicWin')` 在面板按钮进 DOM 之前就跑了，监听器挂了个寂寞。** 联系人/设置/发现/广场当时已在 DOM → 正常。
- **❌ 推翻上轮错误理论**：lines 214-217 的「dumogu 自绘滚动条 overlay 在隐藏面板挂载时算错矩形、轨道盖住 header 三键」经此日志证伪（点击链路上 `BUTTON#courtWinMaxBtn` 完整到达、`main-panel.active` 已就位，capture 监听也证明点击穿透到按钮）。`.chat-header{z-index:1001}` 与 `switchTab` 里的 `rAF sb.update()` 是基于该错误理论的改动，**无害但非必要**，保留不影响；真正修三键的是下面的事件委托。
- **✅ 修复（事件委托，根除时序问题）**：删 `bindWinControls` 函数 + 6 次调用 + cip IIFE 全部 `[DIAG]` 诊断代码；改为在 `document` 上挂**单个** click 委托监听：`e.target.closest('.win-ctrl-btn')` → 按 `btn.id` 派发：`cipWin*` 特殊（close 调 `CipController.close()`），其余 `endsWith('WinMinBtn'/'WinMaxBtn'/'WinCloseBtn')` → `invoke('minimize_window'/'toggle_maximize_window'/'close_window')`。`winMaxBtn` 等主聊天键 id 不以 `Win` 大写开头、不被 `endsWith('WinMaxBtn')` 命中，仍由 145-153 原 per-element 绑定处理，无双触发。无论面板何时进 DOM 都能响应。
- **清理**：删启动标记 console（`[DIAG] initTauri 启动 OK`）、capture 监听、绑定后复查、diagInvoke 等全部诊断；`grep '[DIAG]'/'bindWinControls'/'diagInvoke'` 0 命中；`node --check src/app.js` 通过。
- **待用户 `npm run tauri dev` 实测**：法庭/音乐三键可点并真正改变窗口；其余面板行为不变。未 commit（延续"法庭改好前别 commit"，待用户确认三键 OK 后连同侧边栏卡片/court-pre 字体/滚动条错位一并提交）。

## ✅ 公开法庭整段完工并提交（2026-08-12 17:1x）
- **用户确认三键已可用**（"好的可以了，一起提交，记得去掉多余的调试信息"），本轮法庭工作全部收口，连同前面几段未提交的法庭改动一次性 commit。

### 提交前清理（"去掉多余调试信息"）
- 全量 grep `[DIAG]` / `diagInvoke` / `diagToast` / `courtWinDiag` / `bindWinControls` / `COURT-DEBUG` / `COURT-PROBE` / `court-raw` / `debugger` → **0 命中**，诊断代码已彻底清空。
- 仅保留 `console.log('[Tauri] 已注册 Ctrl+Alt+Shift+F12 切换 DevTools')`（改动前就存在的正常启动日志）与 `console.error('[Tauri] xxx:', err)`（窗口命令失败日志，与主聊天键原有写法一致）。
- 对 diff 新增行做 `alert(`/`测试`/`临时`/`TODO` 扫描：命中项全是面向用户的 `showAlert(...)` 业务提示（投票/陈词/讨论/撤销的成功失败反馈），非调试代码，保留。
- **修正一处写错根因的代码注释**：`switchTab` 里 `subScrollbars` 刷新那段注释原写着"轨道算到 header 区域 → 盖住右上角窗口键导致点不动"，这是已被 capture 日志证伪的旧理论，会误导后人。已改为只描述真实作用（隐藏态挂载时容器矩形错 → 轨道位置/长度算歪 → 面板激活后刷新归位）。

### 保留的两处「基于旧理论但无害」的改动及判定依据
- `app.css` `.chat-header{position:relative;z-index:1001}`：**保留**。全库 z-index 普查确认所有弹窗/浮层/toast 都在 **9998~19000**，只有 dumogu 滚动条 wrapper 是 **999**，故 1001 只压过滚动条层、不会挡任何浮层；语义上 header 本就该盖在内容滚动条 overlay 之上，属合理防御。
- `switchTab` 的 `rAF sb.update()`：**保留**，对"面板隐藏态挂载导致轨道算歪"和"最大化后错位"这类几何刷新确有用（与 `window resize → sb.update()` 互补）。

### 本次提交内容（法庭全量，含此前累积的未提交项）
1. **据真实 schema 重写法庭渲染**：列表加 `?status=all`；详情两层 unwrap（`data` → `case`）+ 从 rawDetail **顶层**取 `merged_reports`/`statements`/`discussions`；并行拉 `/votes`、`/discussions` 两个独立端点；helper `courtStatusLabel`/`courtVerdictLabel`/`courtFmtTime`/`courtPersonHTML`。
2. **写操作对齐官方契约**：投票拆「赞成封禁/赞成保留」发 `{vote,reason,evidence}`；陈词 `{reason,evidence}`；讨论 `{body}`；新增「撤销」按钮 POST `/withdraw`（带 confirm）；四者失败均回显服务端 `error/message` 而非笼统"失败"。
3. **侧边栏案件卡片改版**：双头像（举报人+被告，30px）、标题 `案件 #ID` 居中、举报人/被告与徽章票数自动换行（去掉 slice 截断与 nowrap）。
4. **`court-pre` 字体修复**：加 `font-family: inherit`，不再回退 UA 等宽字体。
5. **窗口三键改事件委托**（本轮核心）：删 `bindWinControls` + 6 次调用 + cip IIFE，改 document 单委托 `closest('.win-ctrl-btn')` → 按 id 后缀派发，`cipWin*` 走特殊分支。根除"面板晚进 DOM 导致监听器挂空"的时序问题。
6. **子滚动条几何刷新**：`subScrollbars[]` 收集实例 + `window resize`（rAF 去抖）+ `switchTab` 激活后 rAF 各刷一次 `update()`。
- 提交：`4d9b4ba` `Fix: 公开法庭渲染重写与窗口按钮事件委托`，3 files changed / 555 insertions / 88 deletions（`src/app.js` / `src/app.css` / 记忆日志），已 push 到 `origin/main`。

### 本轮最大教训（长期价值）
- **窗口/全局按钮一律用事件委托，别 per-element 绑定**：项目里"每次新面板加按钮都点不动"的顽疾，根源就是初始化时机与面板进 DOM 时机不确定。委托一次写好，任何时候插入的按钮都能响应，这是该类 bug 的终极解法（已在本次落地）。
- **诊断要能自证"监听器有没有挂上"**：本轮绕了三大弯（滚动条遮挡 → alert 自毁 mouseup → 怀疑 invoke 到不了 Rust），真正一击定案的是 `bindWinControls(prefix) => min/max/close=true/false` 这条"绑定时元素是否存在"的日志。以后排查"点了没反应"，**第一条日志就该打绑定时元素是否存在**，而不是从遮挡/权限/后端往下猜。
- **写错的代码注释要当 bug 修**：错误结论留在注释里比没注释更坏，会让后人沿错误方向改。定案后必须回头改掉基于旧假设的注释。


