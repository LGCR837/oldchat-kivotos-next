# 2026-08-10 工作日志

- **HAR 分析：用户主页「564 次请求」根因**：解析 `Desktop/新建文件夹 (3)/` 下 3 个 HAR。用户主页 HAR 共 559 条目，但**真正出网的仅 25 次 HTTP**（23 个独立 URL）：profile×1、moments/user×1、friends/requests×1（与看主页无关，白打）、moments/comments×18（N+1，每条动态各回查评论数）、媒体图×3（MediaCache，之后缓存）。其余 507 条 `plugin:http|fetch_read_body` 是**本地 IPC 噪声**：tauri-plugin-http 2.5.9 的 `fetch_read_body` 无 maxSize 参数，每次只回 reqwest 一个 ~1KB 分片，读一张 ~150KB 图 = ~150 次 IPC；3 张媒体图贡献 448 次。这些**不出网、不碰服务器**，只是 DevTools 把它们算进 Network。结论：腐竹的服务器压力来自那 25 次真实请求（尤其 18 次 N+1 + 1 次多余 friends/requests），564 的数字里 ~80% 是本地读图分片噪声。

- **修复用户主页 N+1 评论回查（懒加载，降服务器压力）**：`src/app.js` 两处改动：
  - 动态卡片评论数：服务端给非零计数则原样显示，否则显示 `-`（`app.js:2753` 用 IIFE 包裹 `c>0?c:'-'`）。
  - 删掉打开主页时对每条动态各发 `moments/comments` 的主动回查循环（`app.js` 原 2845-2857），改为 `IntersectionObserver`（root=scroll 容器，rootMargin 120px）懒加载：仅当动态滑入可视区才补查并反写数字；服务端已给非零计数的按钮直接跳过；observer 存 `window.__spCommentObserver` 并在重渲染时 disconnect 防泄漏。效果：打开主页评论回查 18→0；点开/滑到才各查 1 次（与原本要靠点击加载评论一致），功能与视觉不变。node --check 通过。未提交。
  - 待办（用户未明确要求，未做）：`app.js:2691` 每次打开主页无条件拉 `friends/requests` 可改为仅在需判断「待通过好友」关系时才拉（省 1 次/主页）。另 `fetch_read_body` 的 448 次本地 IPC 噪声需 Rust 单发命令（一次性读整个 body）才能压，属独立工作项。

- **发现页新增「系统通知」子页（参考签到墙视觉）**：
  - `src/index.html` 发现页侧栏 `discoverList` 在「签到墙」后加 `data-discover="notice"` 项（bell 图标，名「系统通知」）。
  - `src/app.js` 新增 `renderSystemNotice(target)` + 辅助 `noticePick()` / `extractNoticeList()`；调 `GET /v1/notifications?limit=50`（路径已线上探明：无 token 返回 401、确认存在；`/v1/notice` 是 404），防御性解析兼容 `{notifications:[]}`/`{data:{...}}`/直接数组/首个非空数组等多种容器与多字段名（title/content/created_at/image 及中文候选）。404/异常显示「功能建设中」占位，空列表显示「暂无系统通知」。接入发现页点击分发（`target==='notice'` 分支，加 `discover-has-content` 后渲染进 `discover-main`）。
  - `src/app.css` 加 `.notice-wall`/`.notice-card`/`.notice-head`/`.notice-badge`/`.notice-meta`/`.notice-title`/`.notice-time`/`.notice-body`，白卡圆角+边框+阴影，复用签到墙视觉语言；正文 `white-space:pre-wrap;word-break:break-word`。
  - **待验证**：官方 `/v1/notifications` 返回字段名未在逆向文档（2.16.1 为桩段）中给出，防御性解析已尽量兼容；需腐竹 `npm run tauri dev` 登录后实测校准（若字段名不符，调整 `noticePick` 候选列表即可）。`node --check src/app.js` 通过。未提交。

- **资源广场页面三处对齐其他页面（10:41）**：用户反馈"照着其他页面做"。① **顶栏高度不对**：`plaza-files` 原 `height:100%` 与 `chat-header` 同处 flex-column，总高超出面板导致 flex 把 `chat-header` 压低于 `var(--header-height)`。改为 `flex:1;min-height:0`（与 `.court-detail` 一致）修复。② **三大金刚键缺失**：上一轮误删了 plaza `chat-header` 内的 `header-right > window-controls`（最小化/最大化/关闭）。已按 court 结构补回，id 用 `plazaWinMinBtn/MaxBtn/CloseBtn`，并在 `bindWinControls` 调用处加 `bindWinControls('plaza')`（与 contacts/discover 同模式：prefix='plaza' → 拼出 plazaWinMinBtn，匹配 HTML id）。③ **侧边栏选择样式奇怪**：plaza 分区项原 `.active` 用亮蓝 `var(--blue-600)`+白字，与全站统一的灰底 `.contact-item.active`（`rgba(0,0,0,0.07)`，深色 `rgba(255,255,255,0.12)`）不一致。删掉蓝色 `.active` 覆盖，改用标准灰底（`.plaza-count` 维持 `var(--secondary-text)` 在灰底正常）。node --check 通过。未提交。

- **系统通知页视觉调整（用户指示）**：去掉卡片左侧铃铛徽标图标（移除 `.notice-head`/`.notice-badge`/`.notice-meta` 及 HTML 中 `<i class="fa-solid fa-bell">`），标题直接作为卡片首行；时间 `notice-time` 从标题下方移到卡片底部、改为 `text-align:right` 右对齐（右下角）。仅改 `src/app.js`(renderSystemNotice 卡片结构) 与 `src/app.css`(notice-card 样式)，node --check 通过。未提交。

- **诊断：头像/媒体其实没缓存（用户报告灰色闪烁 + 请求量异常）**：
  - 代码里唯一的"缓存"是 `mediaUrlCache`（`src/app.js`~300 的 `Map`），只 memoize `resolveMediaUrl` 的**字符串结果**，省字符串拼接 CPU，**对网络请求/图片字节零作用**。没有 blob 缓存、没有 `Image()` 预加载注册表、没有任何内存位图缓存。
  - 唯一能缓存图片字节的是浏览器/WebView HTTP 缓存，但两个媒体源都返回 `Cache-Control: no-store`（curl -I 实测 `60.205.94.101:8080` 与 `files.mcl0.dpdns.org` 均 `no-store`）→ HTTP 缓存被禁止存储 → 每个 `<img>` 重新出现都重新下载 → 自己头像第二次出现短暂灰（`background:var(--border)` 占位直到加载）。
  - 媒体候选降级链（`src/app.js`~276 全局 error 监听）默认 4 候选源，任一头像瞬时失败就换源重抓 → 单个头像可发 2~4 请求；叠加频繁整页重渲染（切会话/在线状态/typing/WS 重连都重建 `<img>`）→ 请求量爆炸，符合"腐竹公开数据中位列第一、官方客户端 1.5 倍请求、但用户仅 1/5（≈7.5 倍/用户）"。
  - 结论：需做**客户端真实图片缓存**（fetch→blob: 或 Image() 预热，按原 URL 存 Map，渲染用 blob URL；唯一 URL 每次会话只抓一次；并给离屏头像加 `loading=lazy`）。可同时根治灰色闪烁与请求量问题。尚未实现，待用户确认。

- **修复 MediaCache 原生请求泄漏（紧急，腐竹私信要求降服务器压力）**：
  - 用户此前已加了真正的媒体缓存（`src/app.js`~335 IIFE：IndexedDB + memCache Map + MutationObserver，window.__MediaCache 已 init，白名单齐全，tauriHttpFetch 返回标准 Response）。但**缓存一直在跑却没压住请求**——根因是泄漏：模板把 `<img src="http://60.205…/x.png">` 直接写进 DOM，observer 异步换成 blob，但"换"之前浏览器已向媒体服务器发起原生请求；后端 `Cache-Control: no-store` 让这次请求缓存不住 → 每次重渲染都白打一次原生 http，再被换 blob。媒体源抖动时全局 `error` 监听还会触发候选链（单头像 2~4 请求）。
  - 修复（仅改 MediaCache IIFE，不碰 30+ 渲染点）：① 新增 `MEDIA_PLACEHOLDER`（1x1 透明 GIF），`processElement` 拦截到 `<img src>` 的**第一时间**立即 `setAttribute('src', MEDIA_PLACEHOLDER)` 抢在浏览器原生加载前替换，彻底阻断原生请求；② 候选源降级收进 `fetchAndStore` 内部（`mediaCandidateUrls()` 生成候选 + `fetchOne()` 单源抓取，每个唯一 URL 只抓一次并缓存成功 blob），不再依赖全局 error 监听放大；③ callback 处理：blob→用 blob（零网络），失败→恢复 `val` 远程地址降级为旧行为（全局 error 监听兜底）。效果：每个唯一图片每会话仅 1 次请求（首屏走 blob 也消除灰色闪烁），重渲染零网络。node --check 通过。**已提交并推送**：commit `4a10f1e`「Fix MediaCache native request leak + lazy-load comment counts」，含 MediaCache 修复 + 评论懒加载 + 本周期 UI（QQ Blue/系统通知页/Typing 动画/标题渐变/横向溢出修复），`git push origin main` → `4fa4f49..4a10f1e`。

- **本轮三处「降请求」收尾（已提交推送 commit `2bfbb81`，`4a10f1e..2bfbb81`）**：
  1. **`friends/requests` 不再每次打开主页都拉**：`src/app.js` 主作用域加会话缓存 `_incomingFriendReqCache`（line ~1825）；`load()` 内原 2691 无条件 `apiFetch('/v1/friends/requests')` 改为「缓存为空才拉，否则复用」；`spAddFriend`/`spRespond` 成功后置 `null` 失效以便关系状态刷新。`spRespond`(2894) 与好友申请列表页(5445) 的拉取保留（用户动作/功能本身需要）。效果：看非好友主页由每次 1 次 → 仅会话首次 1 次。node --check 通过。
  2. **全局媒体 error 监听不再放大**：`src/app.js`~277 的 `document.addEventListener('error',…)` 媒体候选降级链，加守卫 `if (img.dataset.mcOrigSrc) return;`——MediaCache 已接管（`data-mc-orig-src` 标记）的图片跳过全局链，因其已在缓存层内部逐候选尝试过一次，避免原生回退时再被放大 2~4 倍。未标记（极少数 MediaCache 未接管的图）仍走旧兜底。node --check 通过。
  3. **文档入仓**：`docs/mcl0-new-docs/`（官方 v2 API 参考 `api202608100558.md`，40K）`git add` 提交。注：`docs/oldchatkivotos.l2.ink/`（官网，57K，functions/public/wrangler.toml）经查**早已被 git 跟踪**（7 文件），记忆中「未纳入 git」已过时，无需重复添加。

- **Bug fix: 视频（ArtPlayer）完全不显示**（`src/app.js`）：下午加的 ArtPlayer 视频消息一直没验证，用户反馈「一片空白、只有个悬浮的暂停按钮」。两个根因：① **容器宽度塌陷**：`.video-message` 用 `max-width:200px;width:100%;aspect-ratio:16/9` 撑高度，但父气泡 `.message.other .message-bubble{width:fit-content}` 让 `width:100%` 解析成 0 → `aspect-ratio` 算出高度 0 → 播放器盒子塌成 0 高，视频不可见，只剩绝对定位的暂停按钮飘着。改为 `width:200px;max-width:100%;aspect-ratio:16/9`（确定宽度，打破 fit-content 循环）。② **MediaCache 误伤 `<video>`**：我的缓存层把 ArtPlayer 内部 `<video src>` 也拦截（先换 1x1 透明 GIF 占位、再异步换整段视频 blob），既打断播放器加载又无意义地把整段视频读进内存。在 `processElement` 加 `if (tag==='video'||tag==='source') return;`，并从 observer 选择器（`img,audio,video,source,track`→`img,audio,track`）与初次扫描里移除 video/source。**关键经验**：MediaCache 只应管 `<img>`/`<audio>` 这类静态资源；`<video>` 等由播放器原生流式加载，绝不能被缓存层接管。ArtPlayer 的 CSS 是自带的（`artplayer.js` 内 `Wt` 变量字符串 + `createElement('style')` 注入 head），无需额外 css 文件。

- **实现 v2 API + 接口版本 4 档开关**（src/app.js）：据官方 docs/mcl0-new-docs/api202608100558.md §4.4 修正 v2 请求头两处 bug 并落地可用 v2。① 删除旧 `v2Enabled`/`oc_enable_v2` 开关，新增 `getApiVersionMode()`（localStorage `oc_api_version`，默认 `v1优先`）；② 修复 `v2SignHeaders(path, method)`：**补 `X-Session`**（handshake 的 session_id，官方强制有效）+ `signingString` 改为 `METHOD+"\n"+PATH+"\n"+TS+"\n"+NONCE`（旧实现错用 token 开头 → 此前必 `bad_signature`/`missing_session`）；③ 重写 `apiFetch` 为「plan 循环」：`v1优先=[v1,v2]`、`v2优先=[v2,v1]`、`仅v1=[v1]`、`仅v2=[v2]`（无 v2 映射直接 `throw` 报错）；每次尝试独立 headers 副本避免 v2 头泄漏；新增 `_fetchVersion`（GET 去重+候选降级）与 `_isFallbackable`（网络/5xx/非JSON-404 才回退，4xx/401 不回退）；端点级熔断 `v2FailedPaths` 保留（仅v2 模式不熔断，让错误上浮）；④ `_fetchWithCandidates` 加 `strictV2` 参数，抑制 仅v2 下的 v2→v1 回退；新增 `window.__wsSession.clear()`，v2 401 含 `invalid_session|missing_session` 时清会话以便重握手；⑤ 设置页「通用」新增「接口版本」下拉（4 选项），切换即时生效并清空 `v2FailedPaths`。X-Enc 信封加密与 /v2/gateway 折叠仍暂缓（可选）。WS 仍走 v1（`/v1/ws`），不随开关迁移。node --check 通过；plan 逻辑用 Node 单测验证 8 例全对。未提交。

- **接口版本默认改为 v2 优先 + 视频播放器放大**（src/app.js，02:07 追加）：① `getApiVersionMode()` 回退默认值 `'v1优先'`→`'v2优先'`（localStorage 未显式设置的用户/全新会话默认走 v2 优先 plan `[v2,v1]`）。注意：此前已手动在设置里选过 v1优先 的会话仍读旧值，需在「设置→通用→接口版本」手动切回才会变；新默认仅影响未存储选择者。② 视频容器 `.video-message` 内联 `width:200px`→`width:320px`（3 处：动态正文 6278、卡片嵌套 6356、嵌套文件 6447），`max-width:100%;aspect-ratio:16/9` 保留，ArtPlayer 不再显挤。气泡最大宽度普遍 280–400px，320px 稳妥不溢出。node --check 通过。未提交。

- **群消息改为按 group_seq 增量续拉**（src/app.js，02:25 实操）：以前每次切进群都 `offset=0&limit=30` 整页拉 + 按 id 去重。现加 `groupLastSeq/groupLastSeqAt/groupLastTs` 状态与 `GROUP_WARM_MS=30min` 热窗口：① **热**（有 DOM + lastSeq>0 + 距上次 <30min）→ 用 `/v2/groups/messages/after?seq=lastSeq`（v2 优先/v1优先 的 v2 分支/仅v2 走此；`仅v1` 回落 `/v1/groups/messages/after?after=<ts>`）增量续拉，仅取 seq 之后、按 seq 升序追加；② **冷**（首开 / 超窗口 / 无 DOM）→ 仍 `offset=0&limit=30` 最新一页，**绝不翻历史**（直接回答"两年没进群会不会全拉"：不会，只拉最新 30 条）。暖拉取失败自动 try/catch 回落冷拉取。③ WS 群消息实时到达时把 `d.group_seq` 写入 msgObj 并 `recordGroupSeq` 推进序号+刷新热窗口；`syncGroupAfterReconnect` 补齐后也 `recordGroupSeq`。新增 `recordGroupSeq()`/`sortMessagesAsc()` 辅助。direct 不动。node --check 通过；纯函数逻辑用 node 单测验证排序 ASC 与冷热判定 4 例全对。未提交。已知限制：暖拉取单批上限 PAGE_SIZE(30)，超忙群单批可能漏中间若干条、需下次进入补齐（与旧 30 页上限同类）。

- **群增量续拉改为「未读数驱动」（替换 30min 时间窗口）**（src/app.js，02:33）：用户指出侧边栏已有未读数，用它判断比时间猜测更准。关键坑：`switchConversation` 在 6125-6130 调 `fetchLatestMessages`(6172/6189) 之前就 `delete unreadCounts[convKey]` 清零，故抓取瞬间 `unreadCounts[convKey]` 永远是 0 → 必须在清零前捕获 `hadUnread = unreadCounts[convKey]||0` 当参数传入 `fetchLatestMessages(type,id,convKey,hadUnread=0)`。新分支（群）：① `hadUnread>0 && hasDom && lastSeq>0 && hadUnread<=GROUP_INCREMENTAL_MAX_UNREAD` → 增量续拉（v2 seq / 仅v1 ts）；② `hadUnread===0 && hasDom` → 已读全、缓存即最新，**直接展示缓存、return 跳过这次网络请求**（比每切一次都重拉整页更省）；③ 其余（首开/无基线/未读爆量）→ 回落最新一页，绝不翻历史。删除死代码 `GROUP_WARM_MS` 与 `groupLastSeqAt`（recordGroupSeq 不再刷时间戳）。`GROUP_INCREMENTAL_MAX_UNREAD` 在 `fetchLatestMessages` 内紧贴 `const PAGE_SIZE=30` 声明、=`PAGE_SIZE`（避免闭包顶部引用 PAGE_SIZE 触发 TDZ 崩溃）；`attachScrollListener(type,id,convKey,PAGE_SIZE)` 的 PAGE_SIZE 是参数非变量，无作用域问题。node --check 通过；分支决策用 node 单测验证 6 场景全过。未提交。已知边界：纯 WS 漏推且未重连时，hadUnread 误为 0 会跳过导致漏看——与本就依赖 WS 的实时视图同源风险，可接受；必要时可加「无未读但仍定期轻拉」兜底层。

- **提交 `7a01c94`（b1f0357..7a01c94，push 到 main）**：本轮整合三块——① 接口版本默认改 v2 优先（`getApiVersionMode` 回退 `'v1优先'`→`'v2优先'`，下拉文案 `v2优先（默认）`，清 3 处残留注释）；② 群消息按 group_seq 增量续拉（30min 热窗口方案，后已回退，见下）；③ 视频容器 `200px`→`320px`。含 `.workbuddy/memory` 同步。工作区随后又进入新一轮改动（未再提交）。

- **【已回退】群消息 seq 增量续拉（用户反馈 v2 接口有 Bug）**（03:11 中断前完成代码，未提交）：用户指示"群聊消息同步改回原逻辑，这个接口有 Bug"。已将 Item 4 全部回退：删除 `groupLastSeq`/`groupLastTs`/`recordGroupSeq`/`sortMessagesAsc`/`useWarm`/`GROUP_INCREMENTAL_MAX_UNREAD`/`hadUnread` 及所有相关调用；`fetchLatestMessages` 群分支恢复为原 `offset=0&limit=30` 最新一页冷拉取（无未读跳过/无增量）；WS 群消息分支移除 `if(d.group_seq) recordGroupSeq(...)`；`syncGroupAfterReconnect` 移除 `recordGroupSeq(...)` 调用；`switchConversation` 移除 `hadUnread` 捕获与传参。node --check 通过，残留引用 grep 为空。**此回退尚未提交。**

- **【已完成未提交】四项新需求进度（03:11 用户叫停，明天继续）**：
  1. **深浅切换移入主题页**（进行中，app.js 已改、待验证）：`renderSettingsAppearance` 删掉"深色模式"条目及其 `settingsThemeToggle` 监听器；`renderSettingsTheme` 顶部加了"外观模式"深浅切换条目（`<div class="settings-item" id="settingsThemeMode">`，点击在 light/dark 间切换并存 `localStorage.theme` + `applyTheme`）。⚠️ 明天需确认：主题页新条目**是否已加 click 监听器**（cb 中断在补监听之前）；若未加，参考原 settingsThemeToggle 逻辑补 `document.getElementById('settingsThemeMode')?.addEventListener('click', …)`。
  2. **发现页资源广场（新页面）**（半途，index.html 已加、JS 未写）：`src/index.html` 三处已加——① 发现页侧栏 `data-discover="plaza"` 入口项（folder-tree 图标）；② 侧栏面板 `<div class="sidebar-panel" data-panel="plaza"><div class="contact-list" id="plazaList"></div></div>`（在 court 面板之后）；③ 主面板 `<div class="court-detail" id="plazaDetail">…空态…</div>`（在 courtDetail 之后）。**JS 完全未写（被中断）**，明天需：a) `switchTab` 接入 `plaza`（保持发现高亮 + 懒加载，仿 music/court 写法）；b) `loadPlazaSections()`：`GET /v1/resources/sections` → `{sections:[{id,name}]}` 渲染左侧文件夹列表；c) 点文件夹 `loadPlazaItems(sectionId)`：`GET /v1/resources/items?section_id=<ID>` → `{items:[{id,name,url,size,bytes,download_url?}]}` 渲染右侧文件列表（名/大小/下载按钮）；d) 下载复用 `downloadFile(url, filename)`（app.js~816 带鉴权 Tauri 保存）；e) 可用辅助 `resolveMediaUrl`/`formatBytes`。API 详见 `docs/mcl0-new-docs/api202608100558.md` §18。
  3. **消息顶部加载转圈**（已完成）：`attachScrollListener` 的 loadHistory 里在 `messagesContainer` 顶部插入 `historySpinner`（`<div class="history-loading"><i class="fa-solid fa-spinner fa-spin"></i> 加载中...</div>`），加载完成（成功/失败 finally）后 `historySpinner.remove()`；`scrollHandler` 也加 `historySpinner?.remove()` 兜底。app.css 加 `.history-loading` 居中 padding + spinner 动画样式（仿 `.court-loading`）。node --check 通过。未提交。
  4. **群消息同步改回原逻辑** → 见上「已回退」，已完成未提交。

  **✅ 本批（四项）已于 2026-08-10 续（~10:25）全部完成并提交**：commit `fe88c48`（7a01c94..fe88c48，push 到 origin/main）。
  - Item1 深浅切换：今早确认主题页 `#settingsThemeModeToggle` 条目 + 监听器（renderSettingsTheme 9575/9593）昨日已完整加上，功能 OK。
  - Item2 资源广场：今早补全 JS——`switchTab` 接入 plaza（保持发现高亮 + `!plazaLoaded` 懒加载 `loadPlazaSections`）；`loadPlazaSections` 调 `GET /v1/resources/sections` 渲染左侧 `#plazaSectionList`，点击分区 `loadPlazaItems` 调 `GET /v1/resources/items?section_id=` 渲染右侧 `#plazaFileList`；文件项用 `.file-download-btn`+`data-dl-url`/`data-dl-name` 复用全局监听（845 行）走 `downloadFile`（带 Bearer，文档 §18 759 行下载需鉴权）；`formatSize` 内联做字节格式化；`index.html` 真实 id 为 `plazaSectionList`/`plazaFileList`（非笔记里写的旧名），并删掉主面板无用的 window-controls；`app.css` 加 plaza 全量样式。防御性解析 `{sections|items}`/`{data:{...}}`/直接数组。node --check 通过。
  - Item3 顶部转圈、Item4 群消息回退：昨日代码已完成，今早一并 commit。
  - ⚠️ 实测待办：资源广场 `save_download` Rust 命令对 `files.mcl0.dpdns.org` 域名是否需补 capabilities 白名单（MediaCache 媒体候选已含该域，但 save_download 是 Rust 侧直连，可能不经 plugin-http 白名单）——若下载报错需查 `src-tauri/capabilities/default.json`。另 v2 资源下载端点 `/v2/resources/download/{itemID}` 文档标 v2，当前 `apiFetch` 在 v2优先/仅v2 模式下若把 `/v1/resources/...` 误判需确认（实际 V1_TO_V2 无此映射，会保持 v1 直发，已验证 OK）。

- **Bug fix: 侧边栏切换标签页面板错位/宽度不一致**（10:38 续）：引入资源广场面板后，`.sidebar-panel` 面板数由 6 变 7，但 CSS 写死 `track width:600%` + `panel width:16.6667%`（=1/6），而 `switchTab` 的 `step=100/total` 动态按 7 算（≈14.286%）→ translateX 与面板实际宽度不匹配，误差随切换次数累积，导致切换标签时面板错位、看起来"侧边栏滚动的宽度不是同一个"。修复：在 `switchTab` 上方按 `sidebarPanels.length` 用 JS 动态设 `track width = n*100%`、每个 `panel width = 100/n%`（覆盖 CSS 写死值，后续再加面板也不会再坏）；CSS 两处写死值改为 7 面板兜底（700% / 14.2857%）+ 注释说明由 JS 控制。`node --check src/app.js` 通过。未提交。

- **资源广场页面三处对齐其他页面（10:41）**：用户反馈"照着其他页面做"。① **顶栏高度不对**：`plaza-files` 原 `height:100%` 与 `chat-header` 同处 flex-column，总高超出面板导致 flex 把 `chat-header` 压低于 `var(--header-height)`。改为 `flex:1;min-height:0`（与 `.court-detail` 一致）修复。② **三大金刚键缺失**：上一轮误删了 plaza `chat-header` 内的 `header-right > window-controls`（最小化/最大化/关闭）。已按 court 结构补回，id 用 `plazaWinMinBtn/MaxBtn/CloseBtn`，并在 `bindWinControls` 调用处加 `bindWinControls('plaza')`（与 contacts/discover 同模式：prefix='plaza' → 拼出 plazaWinMinBtn，匹配 HTML id）。③ **侧边栏选择样式奇怪**：plaza 分区项原 `.active` 用亮蓝 `var(--blue-600)`+白字，与全站统一的灰底 `.contact-item.active`（`rgba(0,0,0,0.07)`，深色 `rgba(255,255,255,0.12)`）不一致。删掉蓝色 `.active` 覆盖，改用标准灰底（`.plaza-count` 维持 `var(--secondary-text)` 在灰底正常）。node --check 通过。未提交。

- **Lua CIP（旧聊 Lua 小程序）真实 API 结构实测（~11:00，带 token curl）**：此前策划的两个未知点已用真实数据澄清，可进 P0 PoC。
  - `GET /v1/discover/lua/manifest`（Bearer token，响应头 `Cache-Control: no-store`）→ `{"apps":[{id,name,description,version,enabled,order,permissions:[...],sha256,script_url}]}`。当前仅 1 个 app：`welcome`（permissions=[camera,network,storage]，sha256=f1b5d811bb67e25dc177197dbe35dab75e2297bf5b31c0b3c31dd35622f1dbf2）。
  - `GET /v1/discover/lua/apps/{id}`（带 token）→ `{"id","version","sha256","script":"<main.lua 源码内联字符串>"}`。**脚本是内联 `script` 字段，不是给 script_url 二次拉取**；`script_url` 是相对路径 `/discover/lua/apps/welcome`（无 /v1），真实拉取端点必须带 `/v1` 前缀（无 /v1 返回 404）。
  - **真实 welcome.lua 揭示的宿主 API 形态**（比 docs/mcl0-new-docs/lua-cip.md 更权威）：`app.storage_get/set`（脚本顶部即调用）、`ui.page({title,children})`/`ui.text`/`ui.image({id,url,height})`/`ui.button({text,on_click=function})`、`app.camera(cb)`、`app.set_image(id,uri)`、`app.toast`、`app.http_get("/me", cb)`。**回调式**：`on_click` 是 Lua function 值、`camera`/`http_get` 是回调式（非 Promise/协程）；渲染器须把 Lua function 捕获并在点击/事件时经 Fengari 回调用。`app.http_get` 路径相对 `/v1`（即 `GET /v1/me`）。`ui.image.url` 是完整 https URL（`https://oc.mcl0.dpdns.org/app-assets/oldchat-icon.png`）。
  - 修正此前策划的两个假设：① 脚本获取=内联 `script` 字段（非 script_url 二次拉）；② URL 拼接=`/v1/discover/lua/apps/{id}`，manifest 的 `script_url` 不可直接拼 origin（缺 /v1）。
  - 沙箱/权限：按 manifest `permissions` 数组在运行时放行 camera/network/storage；拉到 script 后用 sha256 自校验（与 manifest 一致才执行），防篡改。
  - 方向已锁定（用户确认）：完整 MVP + 保留 `os` 安全子集（date/time/clock/difftime，禁 execute/rename/remove）+ 外网 `http_get` 走 Rust 代理命令 `cip_http_get`（allowed_hosts/禁私有网/512KiB/不带 token）。P0 用 welcome.lua 作真实测试样本。

- **【完成·未提交】CIP（旧聊 Lua 小程序）P0 可行性 PoC（Fengari in JS 路线）**：用户指示"先写，然后在设置里随便找个地方加个进入用的调试按钮"。实现并把整条管线离线验证通过。
  - 新增 `src/cip/`：`engine.js`（DOM 无关核心，Fengari 跑 Lua；沙箱禁 os.execute/getenv/rename/remove/setlocale/tmpname/exit + dofile/loadfile；`ui.*` 声明式 DSL ctor，把 `on_click` 等 Lua 函数用 `luaL_ref` 存 registry ref；`app.*` 回调式宿主 API toast/storage_get_set/json/delay/set_text/set_image/http_get/camera/back；指令预算 hook 防死循环；`invokeRef` 经 `lua_pcall` 回调用；`destroy()` 置 `_dead` 让残留定时器/异步回调空转）；`host.js`（`CipHost.makeHost`：`storage_*` 走 localStorage、http_get 经 `window.__cipHttpGet`、camera 桌面返回错误、back 关 overlay）；`render.js`（`CipRender.renderTree/setText/setImage`，按 id 注册表反查、点击经 `ctx.invokeRef(ref)`）；`cip.js`（`CipController`：open/close/refresh 拉 manifest/selectApp/runApp 拉 script+sha256 自校+execute；`window.__cipBackendOrigin`/`__cipHttpGet` 跟随 app.js 后端候选并带 Bearer；`__cipClose`）。
  - `src/assets/vendor/fengari/fengari-web.js`（~1.5MB 浏览器 bundle，已提交）：Fengari 0.1.5 经 browserify 打包；**关键坑**=fengari 用 `typeof process` 区分 Node/浏览器分支，browserify 注入 process polyfill 会错误走 Node 分支→`require('fs')` 炸；补丁=①所有 `typeof process` 守卫钉死浏览器分支 ②摘掉 io/package 库。补丁改在 `scripts/build-fengari.mjs` 的 browserify **global transform** 里做（不落盘改 node_modules，避免 Windows 文件锁 EPERM），含冒烟检查（残留 `require('fs')`/`child_process` 或找不到补丁标记即报错）。`.fengari-build/`/`_cipbuild/`/`_fengari*` 已 gitignore，vendor bundle 必须提交。
  - `src/index.html`：脚本顺序 fengari→engine→host→render→cip→app.js；overlay `#cipDebugger`（标题「Lua 小程序调试器」+ 三大金刚键，关闭键只关 overlay 不关 App）。`src/app.css`：`.cip-overlay*`/`.cip-page*`/各控件/`.cip-toast` 样式。`src/app.js`：设置页「开发者→Lua 小程序调试器」条目（点开 `CipController.open()`）；167-177 行专绑 cip overlay 三大金刚键；`bindWinControls('plaza')`/`('courtWin')` 同前。
  - 离线验证：`_cip_inttest.js`（node 模拟 window=global + mock host）全 13 断言 PASS——fengari 加载、UI 树顺序 text→image→button→input→checkbox→spacer、checkbox.checked 为 bool、on_click 捕获 ref、点击回调触发 app.toast、http_get 异步回调把响应交 Lua、重复 run 不串状态。`node --check` 全部 5 文件 + 构建脚本通过。
  - ⚠️ 待办（未做，P0→P4/P5）：① 外网 `http_get` 尚未接 Rust `cip_http_get` 代理（当前经 `window.__cipHttpGet` 直连，只能打本后端源）；② discover 页集成（把 Lua 小程序作为发现页子项）未做；③ 运行时真实拉 manifest/app 需有效登录 token（此前 token 已过期）；④ 当前 `app.http_get` 在 host 里把相对路径拼 `ORIGIN+'/v1'`，欢迎采样脚本的 `/me` 即走 `/v1/me`。

- **CIP 本地小程序：上传/保存/删除（Fengari 路线扩展）**：用户要求"允许本地上传小程序，支持 .cip/.zip，保存在用户目录，支持删除"。
  - Rust（`src-tauri/src/lib.rs`）新增 4 命令，仿 `import_theme/list_user_themes/delete_user_theme` 范式：① `import_cip_app` 用 `app.dialog().file().add_filter("CIP 小程序",&["cip","zip"]).blocking_pick_file()` 选文件 → 读字节 → 先试 `zip::ZipArchive::new` 解包到 `<app_config_dir>/cip/<id>/`（**防 zip-slip** 的 `safe_join` 逐级拼路径、拒绝 `..`），找不到 `main.lua`/任一 `.lua` 入口则报错；**不是合法 zip 则整文件当裸 Lua 写入 `main.lua`**（兼容把 .lua 改名 .cip 的情况）；按文件名 `sanitize_theme_id` 生成 id，`make_cip_id` 已存在则追加 `_2/_3`；包内若有 `meta.json`/`manifest.json`/`cip.json` 则解析 name/version/permissions，否则派生，写 `meta.json`（`kind:"local"`、entry 相对路径、created_at）。② `list_cip_apps` 遍历 `cip/` 子目录读 `meta.json`。③ `read_cip_app(id)` 按 meta.entry 读入口脚本返回字符串。④ `delete_cip_app(id)` `remove_dir_all`。Cargo.toml 加 `zip = { version="0.6", default-features=false, features=["deflate"] }`（仅支持 deflate/存储，加密/bzip2/zstd 暂不支持）。4 命令已注册进 `invoke_handler`；**无需改 capabilities**——自定义命令本就放行（theme/plugin 同样未列权限）。
  - 前端（`src/cip/cip.js`）：新增 `cipInvoke`（同 app.js `getInvoke()` 写法）；`refresh()` 改 `Promise.allSettled([loadLocal(), loadRemote()])` 合并（任一部分失败不影响另一部分）；列表项加「本地/官方」徽标，本地项右侧加删除按钮（stopPropagation 后 `deleteApp`）；`runApp` 对 `kind==='local'` 走 `read_cip_app` 取脚本直接 `execute`（跳过 sha256 远程校验）。`index.html` overlay 列表顶部加「上传本地小程序」按钮 `#cipUploadBtn`，`open()` 里绑一次点击 → `uploadApp()`（调 `import_cip_app`，用户取消静默）。`app.css` 加 `.cip-upload-btn`/`.cip-badge*`/`.cip-app-del` 样式。
  - 验证：`node --check` 全过；Rust `cargo build` 后台编译中（需联网拉 zip crate）。运行时需在 Tauri 桌面环境实测（上传对话框走原生 file picker）。

- **Bug 修复：下载无反馈（聊天/资源广场文件下载）**（commit `1606a61` + 样式/降级修复 `9071ecc`）：根因是 Rust `save_download`/`save_image` 旧实现「`response.bytes()` 把整文件读进内存 → 最后才弹系统保存框」，前端 `invoke` 期间被完全阻塞。改为：流式下载（`resp.chunk()` 边下边写临时文件）+ `tauri::ipc::Channel` 上报进度（DlProgress{downloaded,total,done,error}）+ 全局 `OnceLock<Mutex<HashMap<task_id, Arc<AtomicBool>>>>` 取消表 + `cancel_download` 命令（前端点取消置位标志，流式循环检测后清理临时文件并返回 Err("已取消")）；下载完成后再弹保存框。前端新增 `showDownloadModal` + `startTauriDownload`（创建 Channel、invoke 带 on_progress+task_id）；`downloadFile` 与 `downloadImage` 的 HTTP 分支改走它。无需新增 Cargo 依赖。
  - **后续两处修正（9071ecc）**：① 用户反馈进度弹窗"风格奇怪"——改为**复用现有 `.custom-modal*` 那套 CSS 类**（overlay/title/body/actions/`.btn`），新建独立节点避免与 `showAlert`/`showConfirm` 共享节点冲突，样式与全 app 一致；删掉整套自写的 `.dl-modal*` 样式，仅留 `.dl-progress/.dl-progress-fill/.dl-info`（进度条用 `var(--accent)`，indeterminate 滑动动画）。② 当时把 `invalid args taskId` 误判为"旧 exe 不认 task_id"，其实**真正的根因是 Tauri v2 参数名大小写**：`#[tauri::command]` 默认要求前端用 **camelCase** 传参，`startTauriDownload` 却传了 snake_case `task_id`/`on_progress`，Tauri 认为缺 `taskId` → 报 `invalid args taskId`；且新二进制 + snake_case 会让主调用与 `_fallbackDownload` 回退双双失败，故"还是报错"。**正确修复（17:30）**：前端改传 `taskId`/`onProgress`（camelCase），Rust 端 `task_id`/`on_progress` 不动（Tauri 自动蛇形转换）。**关键经验**：Tauri v2 命令前端传参一律 camelCase，传 snake_case 会报 `invalid args <CamelName>`；`cargo clean` 是下策（依赖缓存 .rlib 重编要半小时），正常只需 `del` 掉旧 exe 或 `touch lib.rs` 触发重链/重编，且前端改动 tauri dev 热重载无需重编 Rust。本仓库唯一另一处多词参命令 `cancel_download` 的调用也已同步改成 camelCase `taskId`。

- **CIP（Lua 小程序）从调试器 overlay 改造为「发现页独立面板」（18:10）**：用户要求"做成一个正常的页面、标题栏粉色白字、作为单独页面塞进发现（不是发现的子页面）"。做法——① `index.html`：发现侧栏 `discoverList` 加 `data-discover="cip"`「小程序」项（粉头像 `rgba(250,148,166,0.18)`+`#fa94a6`）；新增左侧 `sidebar-panel[data-panel="cip"]`（上传按钮 `#cipUploadBtn`+列表 `#cipAppList`）与右侧 `main-panel[data-panel="cip"]`（`.chat-header` 标题「小程序」+ `#cipWinControls` 三大金刚键 + `#cipRunArea`）；**删除原 `#cipDebugger` overlay**（其组件已迁入上述面板）。② `app.js`：`switchTab` 里 `cip` 走与 music/court/plaza 同款——高亮保持「发现」、首次进入 `cipLoaded` 守卫下调 `CipController.refresh()`；暴露 `window.switchTab` 供 cip.js 跳转；设置页「开发者→小程序」按钮(`settingsCipDebugger`)直接 `switchTab('cip')`（文案由「Lua 小程序调试器」改「小程序」）。③ `cip.js`：`open()` 改为 `window.switchTab('cip')` 导航（不再显隐 overlay）；`close()` 改为 teardown + `switchTab('discover')`；上传按钮绑定从 `open()` 内移到模块加载时绑一次（否则 open 不被调用会导致按钮失效）；ESC 仅在 cip 页面 active 时触发 `close()`。④ `app.css`：`.main-panel[data-panel="cip"] .chat-header { background-color:#fa94a6; color:#fff }`（强制粉色背景+白字，覆盖默认 header 配色；旧 `.cip-overlay*` 死样式保留未删）。node --check 全过；`grep` 确认无 `cipDebugger`/`isOpen` 残留、关键 id 各出现一次、两个 cip 面板就位。未提交。

- **发现页图标配色抽象为主题变量 + 修复 ModernBlock 深色未读徽标白底白字（18:40）**：用户问"为啥小程序图标背景和别的都不一样"（因为上一轮把 cip 头像硬编码成粉，其余 5 个硬编码淡蓝 `rgba(74,158,255,0.15);color:var(--blue-600)`）。改造——① **抽象为 CSS 变量**：`index.html` 去掉 6 个发现页 `.msg-avatar` 的内联背景/颜色；`app.css` 新增规则 `.contact-item[data-discover] .msg-avatar { background:var(--discover-icon-bg); color:var(--discover-icon-fg) }`；三个内置主题各自定义这两个变量——**MomoTalkPink（app.css 默认 `:root`/dark 两块）**=`rgba(250,148,166,0.18)`/`#fa94a6`（粉，保留用户喜欢的粉色、且整组统一）；**QQBlue（app.js `QQ_BLUE_CSS`）**=`rgba(74,158,255,0.15)`/`#4a9eff`（蓝）；**ModernBlock（app.js `MODERN_BLOCK_CSS` 浅+深）**=浅 `rgba(0,0,0,0.06)`/`#555`、深 `rgba(255,255,255,0.12)`/`#ccc`（浅灰）。② **修复 ModernBlock 深色未读徽标白底白字**：根因 `.unread-badge { background:var(--accent); color:#fff }`，ModernBlock 深色 `--accent:#fff` → 白底白字（用户说的"左侧列表的通知"即左栏未读徽标）。改 `color:#fff` → `color:var(--on-accent,#fff)`（`--on-accent` 三主题均有定义：默认/QQBlue=#fff、ModernBlock 浅=#fff 深=#000；ModernBlock 深色下即黑字，修复；其它主题不变；用户主题缺该变量时回退 #fff 保持原行为）。node --check 全过、无内联残留、6 图标齐全。未提交（含上一轮 CIP 页面化改动一并待 commit）。

- **侧边栏「重点分区」置顶分组 + 滑动动画（19:20）**：用户要求"侧边栏顶部加重点分区，所有未读+当前正在聊的会话自动移入，设置可开关，移动时旧位向左滑出屏幕、新位从屏幕外划入、高度变化平滑"。实现——① `renderContacts()`（app.js ~5541）改为**分区结构**：`#contactList` 内放三个 `.contact-section[data-section=group|direct|priority]`，会话项是同一 DOM 节点靠移动归位（不再内联分隔线，改用 `.contact-section-header` 类）；末尾调 `applyPriority(false)` 初始归位。② 重点逻辑（app.js ~5571 起）：`getPrioritySet()`=「unreadCounts[k]>0 或 currentConv.key」；`applyPriority(animate)` 计算旧位 rect → 移动节点（current 置顶）→ 若 animate：跨分区项做**离开-进入分离动画**（克隆旧位 `position:fixed` 向左 `translateX(-(left+width+40))`+淡出，真实项从 `translateX(innerWidth)`+透明滑入到 0；clone 用 fixed 真出屏幕左侧、真实项被 `.contact-list{overflow-x:hidden}` 裁剪从右缘划入）；同分区被挤开的相邻项做**垂直 FLIP**（`translateY` 先反偏移再过渡到 0）使高度变化平滑；`prefers-reduced-motion` 与跨分区项>15 时降级瞬时；异常 catch 兜底清空内联样式。③ 触发点：`updateUnreadBadge` 末尾、`switchConversation` 设 currentConv 后均调 `schedulePriorityApply()`（rAF 合并、关闭时直接 no-op）；设置开关走 `applyPriority(true)`。④ 设置 UI：通用页新增「侧边栏 / 重点分区」开关（`.oc-switch` 复选框 `prioritySectionToggle`，存 `localStorage oc_priority_section`）。⑤ CSS：`app.css` `.contact-list` 加 `overflow-x:hidden`；新增 `.contact-section/.contact-section-header/.priority-section`（标题用 `var(--accent)`+★ 图标）与 `.oc-switch*` 开关样式。纯前端改动，`node --check` 全过；开关默认关，开启后即时重排并播放动画。未提交（多轮改动累计待 commit）。
  - **三项微调（19:33）**：① 重点标题去 ★ 图标、`textContent='重点'` 普通样式（删 `.priority-section .contact-section-header` 粉色+600 覆盖、删 `.fa-star` 规则，直接继承 `.contact-section-header` 的 secondary-text 普通样式）；② **默认启用**：`isPriorityEnabled()` 由 `=== '1'` 改为 `!== '0'`（即只有显式存 '0' 才关，默认开），异常回退 true；③ **"当前已置顶则不移动"**：`applyPriority` 计算 `currentItem` 与 `wasCurrentInPrio`（本次 apply 前是否已在重点区），若 `wasCurrentInPrio` 则跳过其 appendChild（DOM 原地不动）、跳过滑动 `moved` 与垂直 FLIP 两处动画——即当前会话只要已经在重点区就完全静止（首次选中/首次进重点仍正常滑入并置顶）。纯前端，`node --check` 全过。仍累计待 commit。
  - **重点分区改为「活跃度计时模型」（19:45，用户嫌切换太频繁）**：新规则=「所有未读 + 最近打开过的群聊」进入重点；**进入群聊 20s 后才移入**；**闲置 1 分钟（无新消息且未再点开）自动移出**。实现：① 新增模块状态 `prioLastActivity{}`(key→活跃时间戳ms)、`prioOpenTimers{}`(群聊 20s 延迟移入定时器)、常量 `PRIO_ACTIVE_MS=60000`/`PRIO_ENTER_DELAY=20000`；`markPrioActivity(k)` 写时间戳；`getPrioritySet()` 改为「unreadCounts[k]>0 ∪ (now-prioLastActivity[k]<=60s)」（不再把 currentConv 自动纳入）；`prioHeartbeat()` 每 15s 跑——仅当 currentConv 已进入重点(`prioLastActivity[key]` 已存在)才 `markPrioActivity` 保活，并 `schedulePriorityApply()` 驱动过期移出（心跳在 20s 前不 bump 群聊，从而尊重 20s 延迟）。② `switchConversation`：切换前 `clearPrioOpenTimer(prevConvKey)`（离开旧会话取消其待定 20s 定时器，防快速预览被拉入）；新 group 设 20s 定时器，到点 `markPrioActivity`+重排；direct 不设延迟、仅经未读进入。③ `updateUnreadBadge` 当 count>0 时 `markPrioActivity(convKey)`（新消息即活跃、立即进入）。④ `applyPriority` 去掉"current 强制 unshift 置顶"的重新排序以减少跳动（仅保留"已在重点则原地不动"逻辑）。⑤ 设置页说明文案同步更新。纯前端，`node --check` 全过。仍累计待 commit。
  - **时长迭代 + 动画瞬移修复（19:48-19:53）**：① 时长多次按用户要求调整，最终定格 **进入=移除=5s**（`PRIO_ENTER_DELAY=5000`、`PRIO_ACTIVE_MS=5000`），设置页文案同步。② **动画"瞬移"根因与修复**：`applyPriority` 开头 `_resetPrioAnim()` 会清掉所有项的内联 `transform/opacity/transition`，而动画有 480ms 清理定时器——若在此窗口内再次触发归位（消息爆发 `updateUnreadBadge` 连发、15s 心跳撞窗口、或 `renderContacts` 重建列表调 `applyPriority(false)`），新调用会把**正在滑动/FLIP 中途**的项瞬间归位 → 水平或上下瞬移。修复：加**动画锁** `_prioAnimating` + 脏标记 `_prioDirty`——`schedulePriorityApply` 与 `applyPriority` 入口统一 `if (_prioAnimating) { _prioDirty=true; return; }`（**animate=false 路径也要拦**，否则 renderContacts 重建会打断 FLIP）；`_hasAnim` 时置锁并在 500ms 后释放，若期间有脏请求则补跑一次 `applyPriority(true)`（合并爆发）；`catch` 里复位 `_prioAnimating/_prioDirty` 防异常死锁。核心经验：**FLIP/滑动动画必须用锁串行化，任何"清空内联样式"的重置在动画进行中都等于瞬移**。纯前端，`node --check` 全过。仍累计待 commit。
  - **时长改为设置可调、默认 3s（19:56）**：把两个固定常量改为读 localStorage 的 getter——`PRIO_ENTER_LS_KEY='oc_priority_enter_s'`/`PRIO_ACTIVE_LS_KEY='oc_priority_active_s'`（秒，非法/缺省回退 3000ms），`getPrioEnterDelay()`/`getPrioActiveMs()` 各返回 ms；原 `PRIO_ACTIVE_MS`/`PRIO_ENTER_DELAY` 常量删除，两处引用（`getPrioritySet` 的窗口判定、`switchConversation` 的群聊延迟定时器）改调 getter。设置页「侧边栏」区块在开关下新增两个数字输入框「进入延迟（秒）/ 闲置移除（秒）」(`prioEnterInput`/`prioActiveInput`，min 1/max 60 与 300)，`bindPrioTimeInput()` 统一绑定：加载时回填当前值、change 时写 localStorage 并 `schedulePriorityApply()`（缩短闲置窗口时过期项立即滑出），非法值回退 3 并删 key。说明文案改为"默认 3 秒"。纯前端，`node --check` 全过。仍累计待 commit。
  - **时长改为 0~30s 滑块（19:59）**：用户要求"改为 0~30s 的滑块"。① 两个数字输入换成 `<input type=range min=0 max=30 step=1>`（宽 140px，`accent-color:var(--accent)`），旁边 `<span>` 实时显示数值文案（进入：0→"立即"、N→"N 秒"；移除：0→"不自动移除"、N→"N 秒"）。② getter 语义扩展：`getPrioEnterDelay()` 0=立即（`v>=0` 时 `Math.min(30,v)*1000`）；`getPrioActiveMs()` **0=不自动移除（返回 Infinity，永不过期）**、>0 才按秒算、非法回退 3000；新增 `PRIO_MAX_SEC=30` 统一上限（历史遗留 >30 的旧值被钳到 30）。③ 绑定改 `bindPrioTimeSlider(id,valId,lsKey,getter,fmt)`：`input` 事件实时更新文案+写 localStorage+`schedulePriorityApply()`（拖到更小值时过期项实时滑出）；初始化时把 ms 还原成秒（Infinity→0）。说明文案加"进入 0 秒=立即，移除 0 秒=不自动移除"。纯前端，`node --check` 全过。仍累计待 commit。
  - **移除 0s 语义修正：0=立即移除、31=永不移除（20:00）**：用户纠正"移除 0s 也是立即移除啊，移除拉到 31 才是永不移除"。改动：① 移除滑块范围改 `min=0 max=31`（进入滑块仍 0~30）；② `getPrioActiveMs()` 语义：**31→Infinity（永不移除）**、**0→0ms（立即移除，`now-t<=0` 几乎恒假 → 活动型会话下一次归位即被移出，未读型仍按 unreadCounts 保留）**、1~30→秒、非法回退 3000；③ `bindPrioTimeSlider` 增加 `maxSec` 参数（进入传 `PRIO_MAX_SEC`、移除传 `PRIO_MAX_SEC+1`），初始化 `Infinity→maxSec`；数值文案：进入 0→"立即"，移除 0→"立即移除"、31→"永不移除"；④ 说明文案同步"进入 0~30 秒（0=立即），闲置移除 0~31 秒（0=立即移除，31=永不移除）"。纯前端，`node --check` 全过。仍累计待 commit。
  - **默认改为「仅未读」模式：进入31=不自动进入、移除0=立即、重点默认关闭、滑块随开关显隐（20:06）**：用户最终定调——① 文案：移除 0 改显示"立即"（不再"立即移除"）；② 重点**默认关闭**：`isPriorityEnabled()` 由 `!== '0'` 改回 `=== '1'`（只有显式存 '1' 才开）；③ 两个滑块行（`settingsPrioEnter`/`settingsPrioActive`）**仅在重点开启时显示**：新增 `updatePrioSliderVisibility()`（按 `isPriorityEnabled()` 切 display），绑定开关初始+change 时调用；④ 进入滑块也改 0~31，**31=不自动进入**：`getPrioEnterDelay()` 31→Infinity、未设置/非法→默认 Infinity（即默认不自动进入），`switchConversation` 群聊定时器 `if (delay !== Infinity)` 才挂 setTimeout；⑤ **未读不受滑块影响、始终秒进**（getPrioritySet 的 unreadCounts 无条件循环本就如此，无需改动）；⑥ 默认值：进入 31、移除 0（`getPrioActiveMs()` 未设置/非法→默认 0=立即移除）。组合效果：默认开启重点时=「仅未读会话」模式（活动型会话不自动进入、进了也立即被移除）。说明文案同步。纯前端，`node --check` 全过。仍累计待 commit。
  - **修复：移除=0 时活动型会话"还没进入就被移除"（20:10）**：用户报告"移除改到 0 会导致直接无法进入"，并猜测"还没进入就移除了"——**判断完全正确**。根因：`markPrioActivity` 写入 `prioLastActivity[key]=Date.now()`，但真正判定在**下一次 rAF/触发**（≥1ms 后）才执行 `getPrioritySet`；移除窗口=0 时 `now-t=1ms>0` → 被判定"已不活跃"，**一次都没被包含过**就被排除（未读会话不受影响——unreadCounts 无条件循环）。修复：新增 `prioFresh{}`（key→true，"刚进入"标记）——`markPrioActivity` 置位；`getPrioritySet` 活动循环里 `now-t<=w` 不满足时，若 `prioFresh[k]` 则**包含并消费（delete）**，保证刚进入的会话至少被包含一次，之后才受窗口约束。效果：移除=0 时活动型会话能正常滑入、在下一次归位评估时滑出；默认模式（进入31/移除0，仅未读）行为不变。纯前端，`node --check` 全过。仍累计待 commit。

