# 2026-08-09 工作日志

## v1.4.x 适配 — 两条新事实 + uid 出站清单

- **OSS 降级为观察项**：腐竹确认 (1) 后端当前返回相对路径、客户端动态解析，不返 OSS 绝对地址；(2) OSS 能不用就别用。原 P0①（OSS 白名单静默失败）当前不触发。已在 `docs/v1.4.x-adaptation-plan.md` §1.1 加"更新"注记，仍建议保留 capabilities 补 OSS 域名作廉价保险。
- **NCUID 现状**：用户称"大部分接口其实已经可以 ncuid"。产出 `docs/ncuid-uid-sendpoints.md`，列举仍发旧 `uid` 字段的出站点（待问腐竹哪些能改 ncuid）：
  - `to_uid`：发私聊文本(L5668)/红包(L6139)/媒体(L6225) 内联；加好友(L2201/2855/4198) 走 `toUidParam` 助手
  - `with_uid`：标记已读(L2273/2294/4489) 走 `withUidParam` 助手
  - `user_uid`：群邀请(L2691) 字段名特殊
  - WS typing `data.uid`(L3934)；`/v1/me/uid` 改自己ID(L2931，语义拧：接口叫 uid 但存 ncuid)
  - 兜底 `?uid=`：profile(L1976/3444)、moments(L2002)，ncuid 失败后才走
  - 已实现 ncuid/双写（不动）：私聊历史 `?with_ncuid=`、profile/moments 主路径 `?ncuid=`、`mentions` 双写、from 读取兼容
- **实现注意**：`to_uid` 有两套发出方式（内联 + 助手），改双写时发消息三处也要一起改；建议统一双写兼容新旧后端。
- 用户下一步：拿清单去问腐竹哪些端点支持 ncuid，再决定动手。尚未实施代码改动。

## 前端审计：缺失功能 + 体验优化

- 用户转向：腐竹睡了、详细逆向文档未出，空档期先做前端。方向定为"看还缺哪些功能 + 优化前端体验"。
- 产出 `docs/frontend-ux-audit.md`。核心发现（均附代码证据）：
  - **做得好的**：滚动体验扎实、核心聊天增量渲染(`appendMessage` L5158 + convCache DocumentFragment L3003)、图片 onerror 兜底普遍、229 try/catch、57 CSS 过渡。
  - **体验短板（按影响）**：①无障碍 0 处(aria/tabindex/role/alt) ②二级面板 68 处 innerHTML 整片重建(闪烁/丢滚动) ③响应式仅 1 条 @media ④输入防抖 0 处 ⑤加载/空态仅 15 处 ⑥主题不跟随系统 ⑦无快捷键体系 ⑧长会话无虚拟化(待实测)。
  - **体验向缺失功能**：消息发送中/失败重发、转发/多选/置顶/收藏、聊天记录内搜索(v1.4端点已给)、按会话免打扰、emoji 面板/草稿保存、多图预览/语音波形、群公告/群昵称、设备管理、深色跟随系统等。
  - 建议优先级：高(无障碍基线/加载空态/防抖/Enter发送) → 中(列表增量/主题跟随/按会话静音/消息状态) → 低(虚拟化/表情升级/P1功能)。
- 下一步：待用户确认从哪类"低成本高收益"优化动手（未开始改代码）。

## 已实施：@ 提及输入防抖（修复大群卡顿）

- 用户痛点："每次@的时候都一卡一卡的"。诊断：卡顿非网络请求，而是每次 `input` 都在 `filterMentionList` 对全体成员**逐字重算拼音** + 整列 `innerHTML` 重建（L5931 区域）。
- 改动（`src/app.js`）：
  1. 新增通用 `debounce(fn, wait)` 工具（L276 附近）。
  2. 成员加载时**预计算**拼音缓存 `_py`(全拼)/`_ini`(首字母) 到成员对象（`loadGroupMembers` L5903 区 + `refreshGroupMembersCache` L5863 区），`filterMentionList` 优先读缓存，消除每键 O(n) 重算。
  3. `debouncedFilterMention = debounce(filterMentionList, 140)`；消息框 `@` 检测改为"首次打开立即渲染、输入中走防抖"，提及搜索框 `input` 也走防抖。
- `sendTypingStatus` 已有 `TYPING_THROTTLE` 节流，未动；确认全项目仅 2 个 `input` 监听(消息框/提及框)，无好友/音乐按键即查框。
- `node --check` 通过。审计文档 §④ 输入防抖项已实际修复。

## 全新逆向 diff 就绪（oldchat-docs-20260808）→ 对账适配分析

- 旧 `oldchat-docs-20260801/nx/` 整组被删，新增 `docs/oldchat-docs-20260808/`（nx 00~08 + 全新 diff + README + 旧 diff）。新 diff 54KB/18 节（旧版小得多）。
- **NCUID 问题被新 diff 坐实**（用户此前要去问腐竹的"哪些接口能改 ncuid"已可自答）：
  - §7.4 字段映射表 + §7.6 迁移状态表：消息发送/红包/好友操作/群组邀请/已读/聊天设置**全部已迁移到 ncuid**；`to_uid→to_ncuid`、`user_uid→user_ncuid`、`reader_uid→reader_ncuid`、`with_uid→with_ncuid`、`member_uids→member_ncuids`、`friend_uid→friend_ncuid`。
  - §15.3 兼容策略：旧 uid / 新 ncuid / 混合双写 在 v1.4.x 服务端**均工作** → 双写方案获官方级背书。
  - 2 处字段名歧义仍建议腐竹确认（不影响安全，旧字段保底）：加好友新 key 用 `friend_ncuid`（好友删除/拉黑用 `user_ncuid`）；标记已读我们参数名 `with_uid` 平行改名 `with_ncuid`（diff 把已读映射到 `reader_ncuid`）。WS typing `data.uid` 与 `/v1/me/uid` diff 未覆盖 → 暂不动。
  - 已重写 `docs/ncuid-uid-sendpoints.md` 为**决断表**（每项双写决策 + 实施清单）。
- **其它 diff 变化（更新 `docs/v1.4.x-adaptation-plan.md` §0.1）**：
  - `files.mcl0.dpdns.org` 在 v1.4.x **被移除**（§11）→ 我们 `MEDIA_URL_CANDIDATES` 该项过期可删；当前媒体走 `60.205.94.101:8080` 动态解析，无影响。
  - OSS 双协议(http+https)引入，维持观察项（腐竹避用）；保留 capabilities 补 OSS 域名作保险。
  - HTTPS→HTTP 降级 / `/v1` 路径 / URL 配置化：与现有实现一致，不变。
  - 音乐 +8 端点（§6，播放列表 CRUD/同步、详情、歌词、分页 limit30/排序/搜索、排行榜 limit10→50）→ P1 音乐缺口现在有精确端点。
  - 注册重构（§12）：移除应用内 `/auth/register`，我们走 `/v1/auth/web/register` 不受影响（仅回归测试）。
  - Conscrypt(§9)/ECDH(§8)/B站(§8)：零影响不变。
- **下一步**：NCUID 双写实施清单已列（`toUidParam`→`{to_uid,friend_ncuid}`、`withUidParam`→`{with_uid,with_ncuid}`、发消息/红包/媒体内联加 `to_ncuid`、群邀请加 `user_ncuid`）。待用户确认是否动手（2 处歧义非阻塞，旧字段保底）。尚未改代码。

## 实施：6 项前端优化（全部落地，node --check 通过）

### 1. 配置界面候选 URL + 默认优先级降级
- `app.js` L196-265：新增 `DEFAULT_BACKEND_CANDIDATES=['http://oc.mcl0.dpdns.org','https://oc.mcl0.dpdns.org','http://60.205.94.101:8080']`（普通内容优先 oc，降级 60）、`DEFAULT_MEDIA_CANDIDATES=['http://60.205.94.101:8080','http://oc.mcl0.dpdns.org','https://oc.mcl0.dpdns.org']`（媒体优先 60，降级 oc）。`_parseCandidates()` 按 `/[\s,]+/` 拆分 localStorage 的 `oc_custom_base_url`/`oc_custom_media_url`，回落默认值。`refreshEndpoints()` 重算 `BACKEND_ORIGIN`/`MEDIA_ORIGIN`/WS/API/MEDIA 基址。
- `apiFetch`/`_fetchWithCandidates`（L1025-1106）：按 `BACKEND_CANDIDATES` 顺序逐个 `fetch`，网络错误/5xx 降级下一个；401 走 refresh 后重试，仍失败跳登录。
- 媒体图片捕获阶段 `error` 监听（L257）：`<img>` 加载失败按 `MEDIA_CANDIDATES` 顺序自动换源（`dataset.mediaTries` 防环）。
- 设置页 UI（L7391+）：`#baseCandidateList`/`#mediaCandidateList` 渲染为可删 tag，带输入框+「添加」+快捷按钮（`+ oc.mcl0`/`+ 60.205`/`+ oc https`），保存/恢复默认。CSS `.candidate-list/.candidate-tag/.candidate-quick`（app.css L2879+）。
- `login.html` L249：读取 `oc_custom_base_url` 时按 `/[\s,]+/` 取首个候选，兼容多候选存储。

### 2. ncuid 兼容 + uid 降级（双写，获 diff §15.3 背书）
- `toUidParam`→`{to_uid:id,friend_ncuid:id}`；`withUidParam`→`{with_uid:id,with_ncuid:id}`。
- 发私聊文本/红包/媒体上传内联补 `to_ncuid:currentConv.id`；群邀请 `groups/invite` 补 `user_ncuid:uid`。旧字段全部保留作保底。

### 3. 精简请求数
- GET 请求在途去重：`_inflightGet` Map 以 `url|token` 为键，并发相同 GET 共享同一 Response（`res.clone()`，已加 `if(!res)return res` 防 401 登出路径 undefined 抛错）。配合候选降级避免初始化期重复并发 GET。

### 4. 启动白屏 → 闪屏 + 直观自动登录
- `index.html`：`<head>` 加 `.app-splash` 系列 CSS（旋转 spinner）；`<body>` 首位插入 `#appSplash` 闪屏，CSS 立即绘制覆盖 app.js 解析/初始化白屏。
- `app.js`：新增 `hideAppSplash()`（淡出后移除 DOM），在 `DOMContentLoaded` 初始化末尾（L7830 附近）调用；另设 `setTimeout(hideAppSplash,20000)` 兜底，初始化异常也不卡死白屏。
- **未**给 `login.html` 加闪屏（用户意图：登录页应可见、登录按钮自动点击更直观）。`login.html` autoLogin IIFE（L445）：有保存账号密码时 400ms 后 `form.requestSubmit()`（降级 dispatchEvent submit），即“自动进入登录页 + 登录按钮自动点击”。

### 5. 连消息首个(有头像)与第二个(无头像)间距 2x 修复
- `app.css` L700-711：`.consecutive`/`.consecutive-first`/`.consecutive-last` 统一为 `margin-top:0; margin-bottom:2px`（原 `margin-top:-1px/0/4px` 不均）。首个(=consecutive-first)与后续连消息间距现已一致。

### 6. 点击引用消息 → 自动加载历史
- `app.js` 新增 `async jumpToQuotedMessage(quotedId)`（L5453）：若当前页找不到被引用消息，将 `messagesContainer.scrollTop=0` 并派发 `scroll` 事件复用既有“加载更旧”逻辑，最多向前加载 2 页（轮询 `isLoadingMore` 清空 + 60ms 稳定），最后 `scrollIntoView`。引用点击处（L5449）改为调用它。

### 验证
- `node --check src/app.js` → SYNTAX_OK。
- 未做浏览器/Tauri 实跑；HTML 改动（index.html/login.html）仅静态结构，需后续 `npm run tauri dev` 回归：① 候选降级实际生效 ② ncuid 双写服务端接受 ③ 白屏是否被闪屏覆盖 ④ 连消息间距 ⑤ 引用跳历史。

---

## 【严重回归 + 修复】候选降级把 `/v1` 前缀吃掉，全部接口 404

**现象**：用户实跑后所有联网列表全挂，控制台刷屏
`SyntaxError: Failed to execute 'close' on 'ReadableStreamDefaultController': Unexpected non-whitespace character after JSON at position 4 (line 1 column 5) at Object.pull (app.js:101:36)`
（WS init / loadContacts / loadMusicList 全中）。

**根因（已 100% 坐实，非猜测）**：
- 旧代码：`fetch(API_BASE + url.slice(3))`，其中 `API_BASE = BACKEND_ORIGIN + '/v1'` → `origin` + `/v1` + `/me` = `origin/v1/me` ✅
- 我改的 `_fetchWithCandidates`：`fetch(base + url.slice(3))`，而 `BACKEND_CANDIDATES` 存的是**裸 origin（不含 /v1）** → `origin` + `/me` = `origin/me` ❌ **`/v1` 被吃掉**
- 后端（Go + nginx）对未知路由返回 `404 Not Found`，`Content-Type: text/plain`，body 正是 `404 page not found\n`
- `JSON.parse("404 page not found\n")` 的报错逐字为 `Unexpected non-whitespace character after JSON at position 4 (line 1 column 5)`（`404` 解析成数字→index 3 空格→index 4 的 `p` 越界）
- 报错**显示在 `controller.close()`（app.js:101）**是障眼法：Chromium 的 `Response.json()` 是原生实现，在流 close 那一刻同步解析累积字节，JSON 异常被 DOM binding 包装成 `Failed to execute 'close' on 'ReadableStreamDefaultController': <JSON错误>`。**与 Tauri sentinel 字节协议、`res.clone()` 去重都无关**（两者均已单独验证正确，官方 plugin-http 2.5.9 的 Rust `fetch_read_body` 协议确认为 `数据+[0]` / EOF `[1]`，我们的 wrapper 与之一致）。

**实证**：
- `curl http://oc.mcl0.dpdns.org/friends` → 404 text/plain `404 page not found`
- `curl http://oc.mcl0.dpdns.org/v1/friends` → 401 application/json ✅（60.205.94.101:8080 同样表现，两候选均正常）

**修复（src/app.js）**：
1. `_fetchWithCandidates`：`fetch(base + relPath)` → **`fetch(base + url)`**（url 本身已含 `/v1`），删除 `relPath`；401 refresh 后重试同样改为 `base + url`。
2. refresh 端点 `BACKEND_CANDIDATES[0] + '/auth/refresh'` → **`+ '/v1/auth/refresh'`**。
3. 新增 `_normalizeOrigin()`，`_parseCandidates` 统一归一化候选：去结尾 `/`、去误加的 `/v1`、并去重保序 —— 防止用户在设置页粘贴 `http://x/v1/` 再次触发同类问题。
4. 5xx 降级前 `res.body.cancel()`，避免流悬挂。
5. 新增诊断：非 JSON 的 404 直接 `console.error('[apiFetch] 路由不存在（非 JSON 404）…' + base + url)`，下次同类问题一眼可见。

**教训（重要，务必记住）**：
- `BACKEND_CANDIDATES` / `MEDIA_CANDIDATES` 的约定是**裸 origin，绝不含 `/v1`**；拼接一律 `base + url`（url 带 `/v1`）。`API_BASE` 现已无调用方，仅保留兼容。
- 见到 `Failed to execute 'close' on 'ReadableStreamDefaultController': Unexpected non-whitespace character after JSON at position N` 时，**先怀疑响应根本不是 JSON（多半是 404/502 的纯文本或 HTML），而不是流/克隆/sentinel 协议**。`position 4` 基本就是 Go 的 `404 page not found` 指纹。
- 媒体降级 `MEDIA_CANDIDATES[i+1] + src.slice(base.length)` 是整段换 base、保留原路径，无此问题。

---

## 调研：首页列表「折叠群组」是本地还是联网同步

**结论：纯本地 UI 偏好，不联网同步**（逆向文档未逐条写该功能，但架构证据链完整、可定论）。

证据：
1. `mcl0/client.md` §7.1 `ChatsFragment` 分段 = News / Chats / **Folded（折叠的旧会话）**；该分段由 `RecentChatBuilder.buildCombinedList(app, unreadNotifications)` 在**客户端本地**合并算出，数据源只有 `RecentChatCache` + `GroupRecentChatCache` + 系统通知，全程无服务端「折叠」字段参与。
2. `mcl0/client.md` §12.1 SharedPreferences 清单：`settings` SP = `server_base_url / files_server_base_url / 夜间模式 / DPI / `**`首页设置`** —— 与主题、DPI 并列，典型纯本地 UI 偏好。
3. 服务端 `routes.md` 共 **190 条路由，无任何用户级偏好/折叠/置顶/归档接口**。`/v1/me/*` 全是 checkin / presence / devices / profile / avatar / cover / uid / password，没有 preferences。
4. 易混淆项已排除：`POST /v1/groups/settings` = 群管理员改**群配置**（全群生效）；v1.4.x 的 `PUT /friends/settings` = **单个好友**备注/设置。都不是「我的首页折叠状态」。
5. WS 事件只有 `direct_message` / `group_message` / presence / recall，**无偏好同步推送**。

推论：Android 端换设备/重装即丢失折叠状态。我们的 Tauri 端实现直接用 localStorage 即可，与 Android 端天然各存各的、不会冲突。

---

## 官网改造：前身项目 → 本项目（`docs/oldchatkivotos.l2.ink/`）

原站是**前身项目**（Python + Flask 网页版 OldChat for Kivotos）的官网，界面被本项目继承、后端逻辑无关联。按「整体别动、只改关键文本」原则，**只改 `index.html` 文本**，`style.css` / `script.js` 未动。

### 核实到的事实（写站前 curl / API 实测，勿再凭记忆猜）
- **仓库已迁移**：Codeberg `lgcr837/oldchat-kivotos` → **GitHub `LGCR837/oldchat-kivotos-next`**（公开，API 200）。
- **已有 Releases**：最新 **v6**（2026-08-08），每版 5 个产物 —— `windows-amd64/arm64/i386.exe` + `linux-amd64/arm64`，约 12–15MB。→ 本项目实为 **Windows + Linux 双平台**，非仅 Windows（旧记忆需修正）。
- **旧站在线体验 `chat.gweb.work.gd` 已 503**，且本项目浏览器模式已于 2026-08-08 移除 → 该入口必须删。
- `oldchat.online`（OldChat 官网）curl 返回 000，疑与后端半停机维护有关，链接保留未动。
- 仓库**无 LICENSE 文件**；因继承 GPL-3.0 前身的 UI 代码属衍生作品，官网继续标 GPL-3.0（建议补 LICENSE）。

### 实际改动
- title / meta description、navbar brand、footer brand：`OldChat-Kivotos` → `OldChat For Kivotos`（与 `tauri.conf.json` 的 productName 对齐）。
- Hero：副标题改「重写为原生桌面应用」；stat `Python + Flask`→`Tauri v2 + Rust`、`MomoTalk 界面风格`→`Windows / Linux 支持平台`；「在线体验」按钮 → 「下载客户端」(Releases)。
- Features 6 卡全部重写为真实能力（NCUID 适配、无边框标题栏/托盘/通知/启动自检、免安装单文件、富媒体+音乐广场+动态、跨平台多架构）。
- Quick Start 从 `pip install` / `python app.py` 改为：① 下载对应架构 ② 双击运行（Win 需 WebView2 / Linux 需 WebKitGTK）③ 源码构建 `npm install` + `npm run tauri build`。
- 全部 Codeberg 链接 → GitHub；新增 `/releases/latest` 入口。
- **保留未动**：蓝奏云分流下载（`gengcr.lanzouw.com/b00hrpzh9c`，密码 gl4d，200 可达，但那是**前身项目**的文件夹，待腐竹确认是否换新）、蔚蓝档案引用语区块、GPL-3.0。

### 验证
- 标签闭合平衡逐项 OK（div 52/52、a 20/20、p 20/20 等），行数 231→234（仅构建命令多 3 行）。
- 外链全测：GitHub 仓库/Releases 200、蓝奏云 200；无 codeberg/flask/python/gweb.work.gd/localhost:5000 残留。
- 注意：该官网目录**未纳入 git**（`git show HEAD:...` 报 128），无法 diff 回溯，改前需自行备份。

## 官网二轮：按腐竹反馈收窄改动（docs/oldchatkivotos.l2.ink/index.html）
腐竹反馈上一轮**改过头、变丑了**。教训：文案类改动默认只做"事实纠错 + 明确点名的项"，不要顺手重写标题/副标题等品牌调性文案。
- **还原**：hero-badge（去掉「· 桌面客户端」）、大标题（回「为 Sensei 打造的 OldChat 特供版」）、副标题（回「基沃托斯专属的 OldChat 客户端 / MomoTalk 界面风格，让每一位 Sensei…」）。仅把其中主体名换成新名。
- **品牌名定为 `OldChat For Kivotos Next`**（全站 8 处：title/meta/navbar/hero 副标题/about×2/footer×2）。注意 `tauri.conf.json` 的 productName 目前仍是 `OldChat For Kivotos`，**未加 Next，存在不一致**，待确认是否同步。
- **署名顺序**：`LGCR837（Aoharu Reverie）` → **`Aoharu Reverie（LGCR837）`**。**Aoharu Reverie 是腐竹现用网名，LGCR837 是旧名**，以后一律以 Aoharu Reverie 为主。页脚 copyright 同步改为 Aoharu Reverie。
- **删两张功能卡**：「富媒体与音乐广场」「免安装单文件」→ Features 由 6 张减为 4 张（桃信风格界面 / 兼容 OldChat 新协议 / 原生窗口体验 / 跨平台多架构）。
- **「快速开始」整段改为「所有发行版下载」**：section-tag `Quick Start`→`Releases`。**布局结构原样复用**（start-grid / start-card / step-number / code-block / start-links），只是 step-number 由 `1/2/3` 改成 `v3/v2/v1`，h3 为版本标题，p 为该发行版介绍，code-block 内改为**一列 5 个下载链接**（Windows amd64/arm64/i386 + Linux amd64/arm64）。链接 href 暂为 `#` 占位，腐竹后续接后端动态生成。
- **OldChat 官网地址**：`oldchat.online` → **`http://oc.mcl0.dpdns.org/`**（页脚 2 处）。实测根路径 502，系服务器半停机维护中，属预期。
- 导航「快速开始」→「发行版下载」（navbar + footer）。
- **style.css / script.js 全程未改**；5 个下载链接用 inline style（`display:block;padding:3px 0;color:var(--blue-600)`）复用 `.code-block` 容器，避免动 CSS。
- 验证：标签闭合逐项平衡（div 50/50、a 35/35、p 18/18…），发行版链接 15 个（5×3），无 `oldchat.online`/`快速开始`/`Quick Start` 残留。

### 官网收尾 + CF Pages 部署信息（同日第 3 轮）
- **彻底移除「分流下载」**（蓝奏云 `gengcr.lanzouw.com/b00hrpzh9c` + 密码 `gl4d`）：hero-actions 与 start-links 各 1 处，全站已无 lanzou 残留。原因：那是**前身 Flask 项目**的网盘目录，与本项目无关。
- hero「下载客户端」按钮 `href` 由 GitHub Releases 改为 **`#start`**（跳转页内发行版下载区）。`style.css` 第 33 行已有全局 `scroll-behavior: smooth`，**无需加任何 JS**；`.section` padding 80px > navbar 60px，锚点不会被固定导航遮挡。
- 遗留小问题：hero 里「开始使用」与「下载客户端」现在都指向 `#start`，功能重复，待腐竹决定是否把「开始使用」改指 `#features` 或删除。

### Cloudflare Pages 部署（已核实的真实环境）
- **CF 账号已登录**：`gengcr666@outlook.com`，Account ID `d0a115d461c41cd4e3d7e38ed8c294b8`，OAuth Token 存于 `C:\Users\Administrator\AppData\Roaming\xdg.config\.wrangler\config\default.toml`。
- **Pages 项目早已存在**：`oldchatkivotos`，域名 `oldchatkivotos.pages.dev` + 自定义域 `oldchatkivotos.l2.ink`，**Git Provider = No（Direct Upload 模式）**，生产分支 **`main`**。→ 不要新建项目，直接覆盖部署即可。
- wrangler 版本 4.114.0（npx 可直接调用，无需全局安装；有 4.120.0 可升级）。
- **部署命令**（在 `docs/oldchatkivotos.l2.ink/` 目录下执行）：
  `npx wrangler pages deploy . --project-name=oldchatkivotos --branch=main`
  —— `--branch=main` 必须带，否则会被判为 Preview 部署，自定义域不会更新。
- 站点为纯静态三文件（index.html / style.css / script.js，约 30KB），无构建步骤。

### 官网加 Functions API（发行版动态加载）— 已上线
- **目录已重构**（原本三文件平铺，会导致 wrangler.toml 被当静态资源上传）：
  ```
  docs/oldchatkivotos.l2.ink/
    wrangler.toml            # name/compatibility_date/pages_build_output_dir + KV 绑定
    functions/api/releases.js   # GET  -> 读 KV 返回 JSON
    functions/api/refresh.js    # GET  -> 405 "upper(sqrt(-520)) Love You"；POST -> 拉 GitHub Releases 覆盖写 KV
    public/{index.html,style.css,script.js}
  ```
- **部署命令改为**（在 `docs/oldchatkivotos.l2.ink/` 下）：`npx wrangler pages deploy --branch=main --commit-dirty=true`
  —— 有 wrangler.toml 后不用再传目录和 `--project-name`。
- **KV**：namespace `RELEASES`，id `aa4480ecc24e44319db7720cd82c3ee6`，key `releases:latest`。
- **两个坑（已踩）**：
  1. `compatibility_date` 不能写未来日期 —— CF 服务端时钟当时是 2026-08-08，写 2026-08-09 直接 `Deployment failed: Can't set compatibility date in the future`。已改 2026-08-01。
  2. wrangler.toml 放在 `pages_build_output_dir` 内会被上传成静态资源（"Uploaded 4 files"）。必须让输出目录是子目录（`public`）。
  3. GitHub API 未鉴权时限 60次/小时，且 CF 出口 IP 共享 —— 首次 POST 就撞了 403 rate limit。建议配 `GITHUB_TOKEN`：`npx wrangler pages secret put GITHUB_TOKEN --project-name=oldchatkivotos`。
- **前端**：`script.js` 末尾新增加载器，`#releaseGrid` 从 `/api/releases` 取数据渲染卡片；下载直链按 `oldchat-kivotos-next-app-<tag>-<平台>` 规则由 tag 拼出，不走 assets 列表。5 个平台链接实测 206 全部有效。
- 同时删掉「跨平台多架构」功能卡（Features 现 3 张）。
- 线上已验证：GET `/api/refresh` 返回彩蛋(405)、POST 返回 `{"ok":true,count:3,tags:[v6,v5,v4]}`、GET `/api/releases` 正常出 JSON。

### 官网三轮微调（已部署上线）
腐竹 7 条反馈，全部落到 `docs/oldchatkivotos.l2.ink/` 并 `npx wrangler pages deploy --branch=main --commit-dirty=true` 重新部署：
1. **全部加载**：`refresh.js` 改为**自动翻页**（`per_page=100` + page 循环，上限 50 页）拉全量 Release 覆盖 KV；前端 `script.js` 已 `list.map(...)` 全量渲染、无数量截断。当前仓库仅 3 个已发布（非 draft）Release（v4/v5/v6）——「只显示3个」根因是数据只有 3 个，不是代码截断；若后续发布更多会自动全显示。
2. **换行显示**：release body 经 `nl2br()`（先 `esc` 再 `\n`→`<br>`）渲染简介换行（已验证 v6 body 含 `\n`）。
3. **GET/POST 都允许**：`refresh.js` 的 `onRequestGet`/`onRequestPost` 均调 `doRefresh`，**彩蛋文字已移除**（GET 不再返回 `upper(sqrt(-520)) Love You`）。
4. **技术栈文案**：关于卡 `技术栈：前端 (原生 HTML + CSS + JS) · 壳层 (Tauri v2 + Rust)` → `技术栈：前端 HTML + CSS + JS · 后端 Rust + Tauri v2 + JS`。
5. **Hero 技术栈**：`Tauri v2 + Rust` → `Tauri`。
6. **去掉 step-number**：卡片与加载占位、错误卡均移除 `.step-number`。
7. **瀑布流 + 独立下载列表**：发行版简介区改 CSS `column-count` 瀑布流（`.release-masonry`/`.release-card`，响应式 3→2→1 列）；下载链接**移出卡片**，改为下方 `.download-list` 按版本分组的扁平 chip 列表（`.dl-group`/`.dl-link`），不再塞进卡片。
- 部署后实测：首页含 `release-masonry`/`downloadList`/新技术栈文案；GET `/api/refresh` 现返回 `{"ok":true,"count":3,"tags":[v6,v5,v4]}`（CF 边缘有约 1 分钟 Functions 版本传播延迟，首查仍命中旧版彩蛋，重试即新逻辑）。

### 官网四轮：下载链接回卡片 + 镜像中转 + 对齐 + 删 release-tag（已部署）
腐竹对三轮「独立下载列表」不满（"链接这个样式好看，但没让你分开来，还是塞进原本的卡片里面"），并加 4 条：
1. **下载链接移回卡片内**：撤销 `.download-list`/`.dl-group` 独立列表，链接重新作为 `.dl-links` 平铺进 `.release-card`。瀑布流 `.release-masonry` 保留。
2. **镜像中转（纯前端）**：`script.js` 新增 `MIRROR='https://gh.jasonzeng.dev/'`；`assetUrl(tag,suffix)` 先拼 `https://github.com/<REPO>/releases/download/...`，再前缀 `MIRROR`，最终形如 `https://gh.jasonzeng.dev/https://github.com/...`（gh.jasonzeng.dev 是 GitHub 镜像中转，对下载透明）。
3. **OS/Arch 底部对齐**：`.dl-link` 改 `display:inline-flex;align-items:flex-end`，`.dl-os`/`.dl-arch` 同 baseline 底部对齐（原两者顶部错开）。
4. **删 release-tag**：卡片内 `.release-tag`（独立小标签显示 tag）从 `script.js` 渲染与 `style.css` 规则中移除；版本号现仅出现在 `<h3>` 标题（name||tag）。
- 另澄清（非代码问题）：v1/v2/v3 未在 GitHub 发布，故页面只显示 v4/v5/v6，属数据现状。
- 部署：`npx wrangler pages deploy --branch=main --commit-dirty=true`，线上已确认 `script.js` 不再含 `release-tag`、mirror URL 生效。

### 客户端 Bug 修复 1：侧边栏右键菜单隔离（src/app.js）
- 原 `document.addEventListener('contextmenu')` 里只有一个 `.contact-item` 分支，所有侧边栏面板（chat/contacts/music/settings）的列表项都复用同一套「群聊管理/全部已读」聊天式菜单——即腐竹说的"没有隔离，全是聊天界面一样的菜单"。
- 改为**按所属 `.sidebar-panel[data-panel]` 区分**：
  - `chat`：会话级（群聊管理 / 全部已读），保持原行为。
  - `contacts`：发消息 / 群聊管理(仅群) / 复制ID（群）；发消息 / 查看主页 / 复制ID（好友）。
  - `music`：播放(从 `musicData` 按 `dataset.musicId` 取对象→`playMusic`) / 复制链接(`dataset.musicUrl`)。
  - `settings`：导航项，直接 `return` 不弹菜单。
  - 另加 `.sidebar` 兜底守卫（在 contact-item 分支之后）：头部/标签/空白区右击只 `preventDefault` 直接 `return`，**绝不落到聊天面板「刷新」菜单**，确保侧边栏与聊天界面菜单彻底隔离。
- 新增 action：`send-msg`(`switchConversation`+`switchTab('chat')`)、`profile`(`openSpacePanel`)、`copy-id`、`play`、`copy-link`。

### 客户端 Bug 修复 2：兼容 v3 消息格式（src/app.js + app.css）
- 现象：`msg_type:'text'` 且 `body` 是 JSON 字符串（如 `{"text":"...","buttons":[...],"v":3}`）时，原代码仅 `obj.v===2` 有渲染分支，v3 落入 `else` 把**整段 JSON 当纯文本显示**。
- 在 `createMessageElement` 的文本分支新增 `else if (obj.v === 3 || (obj.buttons && Array.isArray(obj.buttons)))`：渲染 `obj.text` + 内联 `.msg-buttons`（Telegram 风格 flex-wrap 按钮）。
- 按钮渲染为 `<button class="msg-btn" data-btn-action data-btn-data>`；因 `escapeHtml` 不转义引号，属性值额外 `.replace(/"/g,'&quot;').replace(/'/g,'&#39;')` 防属性击穿。
- 在 `bubble.innerHTML = content` 之后 `querySelectorAll('.msg-btn')` 绑定点击：`open_url`→`invoke('plugin:opener|open_url',{url})`(失败 fallback `window.open`)；`send_text`/`reply_msg` 等→`sendMessage(data,'text')`。`e.stopPropagation()` 防冒泡。
- 新增 CSS `.msg-buttons`/`.msg-btn`（蓝色描边胶囊按钮，hover/active 态）。
- `node --check` 通过；用样本 body 模拟渲染产出 5 个正确按钮 + 文本，转义无误。
- **未实跑** `npm run tauri dev`（需 Rust/WebView2 长编译）；需用户在客户端回归：① 各面板右键菜单是否隔离 ② v3 按钮消息渲染与点击（发文本/开链接）。

### 官网五轮：横向瀑布流恢复「错位」观感（JS masonry，已部署）
- 腐竹指出纯 `flex-wrap` 是整齐横排、**无错位(瀑布)效果**。为兼顾「横向阅读 + 错位」，`style.css` 撤销 flex 布局，`.release-masonry` 改为 `position: relative`、`.release-card` 去 flex 改绝对定位由 JS 控制。
- `script.js` 新增 `layoutMasonry()`：**按最短列摆放**（经典瀑布流算法），卡片绝对定位 `transform: translate(x,y)`、宽度按列宽算；3 列(>900)/2 列(≤900)/1 列(≤560)。阅读顺序大致左→右（DOM 序入列），视觉恢复 jagged 错位。
- 触发时机：fetch 渲染后 `requestAnimationFrame×2` 再定位（等字体/换行稳定避免高度测算偏差）；`window.resize` 防抖 150ms 重排。
- `node --check` 通过。预览部署 `86808bb2.oldchatkivotos.pages.dev` 已含 `layoutMasonry`×3；自定义域因 CF 边缘缓存约 1 分钟内生效。
